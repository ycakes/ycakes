# ARCHITECTURE.md

Living document. Update whenever an architectural decision is made or changed. This is the source of truth — if code and this file disagree, that's a bug in one of them.

## Stack decisions (locked)

- Framework: Next.js App Router, TypeScript, no separate backend service
- DB/Auth: Supabase (Postgres, Row-Level Security for role enforcement)
- Images: Cloudinary
- Email: Resend (account confirmation only)
- Styling: Tailwind CSS + shadcn/ui
- i18n: next-intl for static UI strings; bilingual product content stored as JSONB per field (e.g. `{ "en": "...", "ar": "..." }`) directly on DB rows. Locale-prefixed routing (`/en/...`, `/ar/...`) via a `[locale]` root segment — applies to storefront **and** admin, per hard rule 6 (no English-only surfaces). `dir` (ltr/rtl) is set on `<html>` per locale in the root layout.
- Cart: Zustand, client-persisted for guests, `cart_items` table for logged-in users
- No Redis, no separate payment gateway, no SMS/WhatsApp API integration (admin-customer contact is manual, via a `wa.me` deep link with prefilled text, not automated)
- Deploy: Vercel (app), Supabase (hosted DB/Auth)
- Source control: GitHub (`github.com/ycakes/ycakes`), connected to the Vercel project for git-push deploys

## Design system (Figma)

The storefront visual design is built and maintained in Figma, not invented ad hoc in code. Claude Code implementing Phase 3 UI should read from this file rather than guessing at spacing, color, or component structure.

- **File**: "YCakes — Design System", file key `UR2u2vVxduNHFheGewn9CH` (`https://www.figma.com/design/UR2u2vVxduNHFheGewn9CH`)
- **Foundations**: color/typography/spacing/radius/shadow tokens live as Figma variables (not hardcoded hex), on the `Foundations` page. Every token has a `var(--...)` web code syntax name matching what should be used in Tailwind config / CSS variables.
- **Components** (one per dedicated page, e.g. `Button`, `Product Card`, `Nav Bar`, `Footer`, `Category Card`, `Price Tag`, `Badge`, `Input Field`, `Filter Chip`, `Quantity Stepper`, `Cart Item Row`, `Color Swatch`, `Topper Card`): each documented with usage notes and, where relevant, known interaction states (hover, selected, disabled).
- **Page mockups** (full assembled pages, not just components): `Home`, `Shop`, `Cake Detail`, `Cart` — each includes Nav + Footer + real (or clearly placeholder) content, composed from the components above as actual Figma component instances, not redrawn copies.
- **Cake Detail page** has two example variants on the same page: the default Normal Cake customization flow, and a second frame ("Cake Detail Page — Fake Cake Variant") showing the Fake Cake flow. A yellow/red annotation note on that page explains the conditional logic in plain language for implementation reference.
- Category and product photography throughout is placeholder (dashed/tinted rectangles) pending real photos from the owner — do not treat placeholder colors as final brand colors for images.

## Auth (Phase 4)

- `profiles.full_name` was split into `first_name`/`last_name` (`20260815140000_profiles_first_last_name.sql`) — every Phase 4 mockup (Register, Checkout, Profile) treats them as separate fields, so a combined string split on whitespace later would be fragile. `handle_new_user()` now reads `first_name`/`last_name` out of `auth.users.raw_user_meta_data`, populated via `supabase.auth.signUp({ options: { data: { first_name, last_name } } })`.
- **Email confirmation flow**: signUp() with confirmation required returns no active session until the user clicks the emailed confirmation link — so Register's optional Address/Phone entries can't be inserted at signup time (RLS requires `auth.uid() = customer_id`, and there's no `auth.uid()` yet). Confirmed approach: any addresses/phones entered during Register are held client-side (sessionStorage) until the confirmation link completes and lands the user in an authenticated session, at which point they're submitted then. A `/auth/confirm` route handler exchanges the emailed code for a session.
- Saved-address/phone data doesn't survive a different device/browser confirming the email than the one that filled out Register (sessionStorage is per-browser) — accepted limitation, not solved this phase. Worth a "your addresses didn't come through, add them from Profile" fallback message if the sessionStorage read comes back empty post-confirmation.
- **Implemented**: `/register`, `/login`, `/register/complete` (client components, `src/app/[locale]/(storefront)/...`), `/auth/confirm` (Route Handler, `src/app/auth/confirm/route.ts` — deliberately outside `[locale]` since it's a link target from an email, not app navigation; `proxy.ts`'s matcher excludes `/auth` so next-intl doesn't try to locale-prefix it). `src/lib/supabase/client.ts`/`server.ts` (already existed) are used as-is — browser client for signUp/signInWithPassword/inserts, server client for `verifyOtp`.
- **Resend integration**: `supabase/functions/send-email/index.ts` is a Supabase Auth "Send Email" hook (Standard Webhooks-signature verified) — Supabase calls it instead of sending its own email, and it sends the real confirmation email via Resend (bilingual, driven by `user_metadata.locale` set at signUp). Confirmation link points at `/auth/confirm`. **Deployed and wired** (function deployed, `RESEND_API_KEY`/`SEND_EMAIL_HOOK_SECRET` secrets set, hook enabled in the dashboard, `enable_confirmations` on).
- **Known limitation, blocked on Phase 1's still-open "Connect custom domain" task**: the `from:` address is Resend's shared sandbox sender (`onboarding@resend.dev`), which can only deliver to the email address on the Resend account itself (`ycakesnet@gmail.com`) — any other recipient is silently rejected by Resend, surfacing as a generic signUp() failure. Real customers can't receive confirmation emails until a domain is purchased, connected, and verified in Resend (Domains → Add Domain → add the SPF/DKIM records), at which point `send-email/index.ts`'s `from:` should be swapped to an address on that domain.
- **Temporary: "Enable email confirmations" is OFF** (owner decision, dashboard-only toggle under Authentication) until the domain/Resend verification above is sorted — `signUp()` returns an active session immediately with no email step. Register's code handles both states without needing further changes: it checks whether `signUp()` returned a session (`data.session`) and, if so, syncs any addresses/phones immediately and redirects home instead of showing the "check your email" screen; if not (confirmations back on later), it falls back to the sessionStorage + `/register/complete` flow described above.
- Password fields enforce a client-side 8-character minimum (`Register`'s `MIN_PASSWORD_LENGTH`) ahead of Supabase's own `minimum_password_length` check.
- `ToggleChip` (`src/components/storefront/ToggleChip.tsx`) is a new stateful button-based chip for the Call/WhatsApp/Both contact-method picker — the existing `FilterChip` is `Link`/URL-driven (built for Shop's category filters) and isn't usable inside a form, so this is a separate component rather than overloading that one.
- `InputField` (`src/components/storefront/InputField.tsx`) gained a `type` prop (`text`/`email`/`tel`/`password`) and, for `password`, a show/hide eye-icon toggle (hidden by default) — used by Login/Register and will be reused by Checkout's inline Register tab.

## Roles

- **Guest** — no account, can browse and order, gets a UUID-linked order visible only to admin
- **Customer** (optional account) — same as guest plus saved addresses/phone, order history tab
- **Admin** (2-3 accounts) — full access: catalog, orders, promo codes, delivery config, expenses, analytics, role/user management. Admins can also manually register orders that were placed **off-platform** (phone, Instagram DM, in-person) so business analytics reflect the whole business, not just website orders — see "Manual/offline order entry" below.
- **Accountant** (1 account) — orders + business analytics + expenses only, no catalog/category control. Read-only access to catalog tables (cakes, categories, sizes, tiers, flavors, colors, shapes, toppers and their join tables) so analytics can resolve order line items to human-readable names instead of bare IDs — unfiltered by `active`, so a since-disabled item still resolves for historical orders. No catalog write access.
- **Phase 7 RLS decision — resolved, owner confirmed (B)**: the Accountant role has **no** read policy on `customer_addresses`/`customer_phones` (deliberate Phase 4 decision — "address book isn't accountant's domain") and that stays as-is. The Customers List page (linked from Analytics, which Accountant can otherwise reach) gates its address/phone Expand panel specifically to `requireAdmin`, even though the rest of `/admin/analytics` uses `requireStaff`. Order history within that same panel is fine for Accountant, since Accountant already reads `orders` directly.

All role-gated access enforced via Supabase Row-Level Security, not just UI hiding.

## Core entities (implemented in Phase 2 — see `supabase/migrations/`)

- `categories` — **birthday, wedding, graduation, bento, custom, candy corner** (+ Candy Corner subcategories: **cupcakes, pops, popsicles, dessert cups** — 4 subcategories, not 3). Admin manages fully: create/edit/delete/reorder.
  - **"Fake Cakes" is no longer a category.** It was removed as a top-level category and reworked as an order-item-level attribute — see "Fake Cake ordering" below. The original Phase 2 seed's `fake` row was deleted in the Phase 2 follow-up migration (`20260814100100_remove_fake_category.sql`).
- `cakes` — belongs to category, bilingual name/description, images (Cloudinary), one primary image
- `sizes` — per category; normal cakes sized by serving count, candy corner items by quantity. Cupcakes uniquely start at 6, then all four Candy Corner subcategories (cupcakes, pops, popsicles, dessert cups) proceed in 12-unit steps (pops/popsicles/dessert cups start at 12); max up to 996 (the largest multiple of 12 not exceeding 1000)
- `flavors`, `colors`, `toppers` (custom cakes only, some toppers have color variants) — admin managed, can be disabled temporarily
  - `category_flavors` (`category_id`, `flavor_id`, `20260815110100_category_flavors.sql`) opt-in restricts which flavors are selectable per category — **no rows for a category means unrestricted** (all flavors, today's behavior for Birthday/Wedding/Graduation/Bento/Custom), rows present means only those. Seeded: Cupcakes/Pops/Popsicles → Vanilla or Chocolate only (single choice — Candy Corner never has a 50/50 split regardless, see Tiers below); Dessert Cups → its own 7-flavor list (Oreo, Chocolate, Lotus, Ferrero Rocher, Tiramisu, Red Velvet, Cheesecake), four of which (Lotus/Tiramisu/Red Velvet/Cheesecake) are new `flavors` rows added in the same migration. Those four aren't excluded from other categories' unrestricted lists — the restriction is opt-in per category, not a global exclusivity rule.
- `orders` — status: Pending → Confirmed / Cancelled → Completed / Cancelled; guest (UUID + name) or linked to customer account; delivery or pickup; delivery/pickup date checked against `delivery_calendar_blocks`. `customer_id` is `on delete set null` — deleting a customer account never blocks on order history and never cascades it away; the order just reverts to guest-shaped (customer_id null), same as it would look if placed without an account.
  - `source` (`website`/`phone`/`instagram`/`in_person`, default `website`) distinguishes manually-entered offline/Instagram orders from real website orders in reporting, while still being included in all revenue/volume/AOV analytics by default. The `normalize_order_on_insert` trigger forces `source = 'website'` on every insert today, since the only insert path is the customer-facing flow — Phase 6 manual entry will need its own privileged insert path that can set the other values.
- `order_items` — cake + all chosen customization. For a **Normal** cake: size, color(s), shape, flavor(s), 50/50 flag, text on cake, text on board, topper, additional notes, price starts as a base estimate and is finalized by admin. For a **Fake** cake, see "Fake Cake ordering" below — several of these fields don't apply and different ones are used instead; `size_id`/`shape_id` are nullable for this reason, with a `order_items_fake_cake_fields` check constraint enforcing the two field sets stay mutually exclusive.
  - Icing color is **multi-select** (owner request, Phase 3): `order_items.color_id` was dropped in favor of `order_item_colors` (`order_item_id`, `color_id`, `sort_order`), mirroring the existing `order_item_flavors` join-table pattern — any number of colors per item, no cap. RLS/audit-log wiring mirrors `order_item_flavors` exactly (`20260815100000_order_item_colors.sql`).
  - When more than one color is picked, the storefront shows a dedicated `color_arrangement_notes` text field (nullable at the DB level, `order_items`, `20260815110000_color_arrangement_notes.sql`) — kept separate from the general `notes` field on purpose, not mixed into it. **Required at the UI level** whenever 2+ colors are selected (blocks add-to-cart like any other required field), even though the column itself is nullable — nullable because it's meaningless/absent for the single-color case, not because it's optional when multi-color. The field/textbox itself stays mounted (not unmounted) when toggling back down to 1 color so typed text isn't lost by an accidental deselect, but its value is only carried into the `CartItem`/order submission when 2+ colors are still selected at add-to-cart time.
  - 50/50 flavor split is only offered when the selected size has tiers available (`size_tiers` non-empty for that size) — this already naturally excludes Bento and every Candy Corner subcategory (neither ever has `size_tiers` rows) without needing a separate category flag, and for Birthday/Wedding/Graduation/Custom it lines up exactly with the owner's "24>30 or bigger" rule since that's the same threshold tiers unlock at.
  - `reference_image_url` is available for **every** order item, not just Fake Cake — the schema never actually restricted it to `is_fake = true` (only `order_items_fake_cake_fields` gates `size_id`/`shape_id`/`fake_size_cm`/`fake_shape_id`), so Phase 3 UI now shows the upload field unconditionally. **Now a real Cloudinary URL, uploaded at file-selection time** — see "Customer-facing reference-image upload" below (was `URL.createObjectURL`-only through Phase 3/4/early Phase 5).
  - **Phase 7 note for Catalog Detail's topper usage list**: verify the exact column/table name for an order item's topper selection in the live schema (`order_items.topper_id` is assumed here based on the "Toppers" single-selection description in Fake Cake ordering below, but was never spelled out as its own bullet in this doc — confirm against the actual migration before writing the aggregation query).
- `promo_codes` — code, fixed/percentage discount, min order, expiry date, total redemption cap, unlimited-per-customer use while active
- `promo_code_redemptions` — tracks usage against the cap
- `delivery_areas` — area name + delivery price, admin managed
- `delivery_calendar_blocks` — dates admin has closed for both delivery and pickup
- `expenses` — amount, date, category (admin-managed categories), free entry, backdatable
- `expense_categories` — **Phase 7 note**: this table gets its first admin CRUD UI this phase (the "Manage Categories" modal, see Phase 7 section). Verify it already has a bilingual `name` (JSONB `{en, ar}`) matching every other catalog-style table's convention — add a migration if it's still a plain `text` column from its original Phase 2 seed, since every other admin-managed name field in this project is bilingual and Manage Categories will need EN/AR inputs.
- `audit_log` — who changed what, when, on every admin/accountant mutation. **No admin-facing viewer exists or is planned** (cut from Phase 8's original scope, owner decision) — the table and its `fn_audit_log()` triggers keep running as-is (every table listed in `20260813121000_audit_log.sql` is still audited on every insert/update/delete), the data just has no UI to browse it. `audit_log_admin_read` RLS (admin can `select`) is also still in place, unused by any page today.
- `newsletter_subscribers` — phone numbers that opted in to WhatsApp campaign updates

## Fake Cake ordering

Previously "Fake Cakes" was planned as its own top-level category. That's been reworked: **Fake Cake is now a per-order-item cake type toggle**, not a category.

- **Where it appears**: on the Cake Detail / customization page, a "Cake Type" selector (Normal Cake / Fake Cake) shows at the very top, above size selection — for every category **except Bento and Candy Corner** (those two stay real-cake-only; a display-only fake bento or fake candy-corner item isn't a real use case for this business).
- **Two ways to reach a Fake Cake**: (1) built from scratch via the Custom Cakes category, or (2) picked as a fake/display version of an existing listed design in another category (e.g. browsing Birthday Cakes, then choosing a specific cake and marking it Fake instead of ordering it real).
- **When Fake Cake is selected, the customization fields change entirely** — no serving-size, no tiers, no flavor, no 50/50 split (a display cake isn't edible, so flavor is meaningless):
  - **Size in centimeters** (free text/number field, not the servings-based size picker used for real cakes)
  - **Icing color** (same Color Swatch picker as real cakes — multi-select for both, see `order_item_colors` in Core entities above)
  - **Shape** — restricted to **Rectangle and Circle only** (real cakes offer a broader shape set including Round/Square/Heart/etc. — fake cakes deliberately don't)
  - **Text on cake**, **text on board** (same as real cakes)
  - **Additional notes** (same as real cakes)
  - **Reference image** (optional upload — a photo of what the display cake should look like; also available for real cakes in Phase 3, not fake-exclusive — see Core entities above)
  - **Toppers**: **only shown when the fake cake was built via the Custom Cakes category.** If it's a fake/display version of an existing design from another category, no topper section appears at all.
- **Schema (implemented, `20260814100200_order_items_fake_cake.sql`)**:
  - `order_items` columns: `is_fake` (boolean, default false), `fake_size_cm` (numeric), `fake_shape_id` (uuid, FK to `shapes(id)`), `reference_image_url` (text, nullable — not Cloudinary-specific at the schema level; Phase 3 currently populates it client-side only, see Phase 3 implementation notes below).
  - `fake_shape_id` restriction implemented via a `shapes.fake_eligible` boolean flag (true for Rectangle/Circle only) rather than a dedicated enum — reuses the existing `shapes` table as the single source of truth. A trigger (`order_items_validate_fake_shape`) enforces `fake_shape_id` points to a `fake_eligible` row, since that can't be expressed as a plain single-table check constraint.
  - `size_id` and `shape_id` on `order_items` are now nullable. The `order_items_fake_cake_fields` check constraint enforces the real-cake vs. Fake Cake field sets are mutually exclusive: real cakes require `size_id`/`shape_id` and forbid the fake fields; Fake Cake items require `fake_size_cm`/`fake_shape_id` and forbid `size_id`/`shape_id`/`tier_id`, with `is_fifty_fifty` forced false. Flavor selection (`order_item_flavors`, a separate table) isn't DB-constrained by `is_fake` — that's left to application logic when Phase 3/4 code is written.
  - `is_fake = true` is rejected at the DB level for Bento and Candy Corner (including its subcategories) items via a trigger (`order_items_validate_fake_category`), since the check spans `order_items` → `cakes` → `categories` and can't be a plain check constraint either.

## Tiers

Birthday, Wedding, Graduation, and Custom cakes can have multiple tiers at larger sizes: no tier choice below the 24>30 size, tiers {**1**,2,3} available at 24>30, tiers {**1**,2,3,4} available at 34>40 and every larger size. Bento never has tiers. Candy Corner never has tiers. **Fake Cake items never have tiers**, regardless of category — tiers are a real-cake-only concept.

- **`tier_count = 1`** (owner request, `20260815110200_single_tier_option.sql`) represents "just a normal, non-tiered cake" as an explicit, normally-preselected option alongside 2/3/4 — rather than requiring the customer to add extra tiers just because the size happens to qualify. The `tiers.tier_count` check constraint was widened from `between 2 and 6` to `between 1 and 6`; the storefront auto-selects the 1-tier option whenever a qualifying size is picked, leaving 2/3/(4) as alternatives.

## Candy Corner size input (Phase 3 note)

Cupcakes/Pops/Popsicles/**Dessert Cups** sizes are discrete quantity rows (cupcakes: 6, then 12-step increments to 996; pops/popsicles/dessert cups: 12-step increments to 996), not a free-typed number. Implemented as `SizeQuantityInput` (`src/components/storefront/SizeQuantityInput.tsx`): typing an in-between number is treated as invalid and blocks add-to-cart (shows which two valid sizes bracket it, e.g. typing 9 shows "Must be 6 or 12"; typing below the smallest size shows "Minimum is 6" instead). Custom +/− stepper buttons move between valid sizes (6→12→24…), not by 1 — native number-input arrows can't express a variable step.

## Business analytics (owner-facing)

Revenue (from completed orders' final price) by day/week/month/year and custom date range, order volume, average order value, most-ordered cakes/flavors/sizes over a period, delivery area breakdown, promo code usage, cancelled order list, expenses by category, net profit (revenue − expenses) — with Excel export on all of the above. **Fully specified and designed in Phase 7 below** — this bullet is the original Phase 3-era placeholder note, superseded by the Phase 7 section.

**All of the above must include manually-entered offline/Instagram orders alongside real website orders by default** (see `orders.source` above), since the owner needs whole-business numbers, not just the website's slice of it. Reports may optionally allow filtering by source later, but the default view is combined.

## Manual/offline order entry (Phase 6 — implemented)

See the Phase 6 section below (`create_manual_order` RPC) — this used to be a forward-looking note, now implemented.

## Folder structure (Phase 1)

- `src/app/[locale]/(storefront)/...` — public site, mounted at `/`
- `src/app/[locale]/(admin)/admin/...` — admin dashboard, mounted at `/admin` (route group name doesn't affect the URL; the `admin` path segment does)
- `src/i18n/` — next-intl `routing.ts` (locales, default locale), `navigation.ts` (locale-aware `Link`/`redirect`/router), `request.ts` (message loading)
- `src/proxy.ts` — locale-detection proxy (Next.js 16 renamed the `middleware.ts` convention to `proxy.ts`; same edge-runtime mechanism)
- `messages/en.json`, `messages/ar.json` — static UI strings
- `src/components/`, `src/lib/` — shared across storefront and admin (shadcn/ui components live in `src/components/ui/`)
- `src/lib/supabase/client.ts` (browser) and `server.ts` (Server Components/Actions, via `@supabase/ssr` + `next/headers` cookies) — session-refresh logic in `src/proxy.ts` deferred until Phase 4 (auth), no auth flow exists yet
- Hosted Supabase project ref: `yddapkhhniecjnnzrolv` (region eu-central-1). Only the anon/publishable key is used client + server side; `service_role` is never used — RLS handles authorization per [Roles](#roles)

## Storefront pages (Phase 3 — all built)

Home (`/`), Shop/category browse (`/shop`, `/shop/[category]`), Cake Detail (`/cakes/[id]`, customization flow with Normal + Fake Cake variants), Cart (`/cart`). **No separate Contact page** — don't build a `/contact` route. "Contact" in the nav and footer no longer scrolls to the footer (superseded, Phase 4 follow-up): it opens `ContactModal` (`src/components/layout/ContactModal.tsx`, Base UI `Dialog`) — phone number with Call (`tel:`) and WhatsApp buttons, plus an Instagram link, all opening in a new tab except Call. Footer's separate always-visible "Get in Touch" section (WhatsApp + Instagram inline) is unchanged, just now reads from the same shared constants.

- **Contact info is centralized in `src/lib/contact.ts`** (`CONTACT_PHONE_DISPLAY`/`CONTACT_PHONE_TEL`/`CONTACT_WHATSAPP_URL`/`CONTACT_INSTAGRAM_URL`) — the phone number is still a placeholder (`+20 100 123 4567`) pending the owner providing the real one; swap it in this one file once known, everything else (modal, Footer) reads from it.

## Nav Bar (Phase 3 follow-up, confirmed post-Figma-review)

- The four nav links (Home/Shop/Custom Cakes/Contact) are now **centered** on the page — previously `justify-between`-style distribution skewed them off-center toward the wider action-icon side (language toggle/cart/etc.). Fixed as part of the Phase 4 design session's Nav Bar component update.
- A **Profile icon** was added to the nav actions area, positioned between the language toggle and the cart icon, styled to match the cart/language-toggle (bordered circle, `bg-bg-surface`, shadow).
- **Session-aware** (`src/hooks/useSession.ts`, a thin wrapper over `supabase.auth.getSession()` + `onAuthStateChange`, shared by NavBar/Login/Register): logged out → plain link to `/login`. Logged in → a Base UI `Menu` dropdown (`@base-ui/react/menu`, already a dependency via `Button`) with **Profile** (links to `/profile`, not built yet) and **Log Out** (calls `supabase.auth.signOut()`, redirects home). No separate dropdown library was added — Base UI's headless `Menu` primitive covers it.
- Login and Register both redirect to `/` if a session already exists (checked via the same `useSession()` hook) — a logged-in user has no reason to see either page.

## Phase 4 — Checkout & accounts (Figma design confirmed)

Design status: Figma design complete for Checkout, Order Confirmation, Register, Login, and a new Profile page (all added in this session), plus the updated Nav Bar component covered above. File: "YCakes — Design System", key `UR2u2vVxduNHFheGewn9CH`, pages `Checkout`, `Order Confirmation`, `Register`, `Login`, `Profile`.

### Checkout page (`/checkout`)

- **Layout**: main column (auth tabs, contact details) + sidebar (right column) with **Fulfillment Details recap above Order Summary** — fulfillment recap is no longer a main-column section, it moved to the sidebar.
  - Fulfillment recap's "Edit" action opens an **inline modal** (Pickup/Delivery toggle, delivery area chips, date picker) rather than navigating back to `/cart`. The modal reuses the existing `DatePicker.tsx` and delivery-area chip logic already built for the Cart page (`src/store/cart.ts`, `delivery_areas` table) — not a reimplementation.
- **Auth**: a 3-way tab switcher (**Guest / Log In / Register**) sits inline at the top of Checkout, replacing the earlier simpler Guest/Create-Account toggle.
  - Switching tabs must never clear anything already typed in Contact Details below — Contact Details state is independent of which auth tab is active.
  - On successful inline login/register (without navigating away from Checkout, cart/session state preserved throughout), any saved addresses/phone numbers on the account appear as selectable chips that auto-fill Contact Details.
- **Contact Details** (expanded from the earlier simpler version):
  - Row: First Name + Last Name + Company (optional)
  - Address
  - Apartment/Suite (optional)
  - Phone Number 1 and Phone Number 2 (optional), each paired with a **contact-method preference** (Call / WhatsApp / Both)
  - Email (optional)
  - **"Save Address" button** (shown only when logged in) saves name/address/phone to the account in one action — replaces the earlier per-field "save" checkbox idea.
- **Order Summary**: Promo Code is now part of Order Summary, not a separate section — input + Apply button + applied-state feedback live directly under Subtotal.

**Implemented** (`src/app/[locale]/(storefront)/checkout/page.tsx` → `CheckoutPageContent`): the design above, plus real (not stubbed) Promo Code validation and Save Address writes.

- `EditFulfillmentModal` + `FulfillmentFields` (`src/components/storefront/`): the fulfillment picker was extracted out of `CartPageContent` into `FulfillmentFields`, a plain controlled component (locale/deliveryAreas/blockedDates + value/onChange props), used directly by Cart (unchanged behavior) and wrapped in a Base UI `Dialog` by `EditFulfillmentModal` for Checkout. Both read/write `useCartStore` directly — no staged/local copy — matching the confirmed decision that Cart and Checkout must never disagree on fulfillment state. `Cart`'s "Proceed to Checkout" now navigates to `/checkout` (`CHECKOUT_ENABLED` flipped `true` in `src/store/cart.ts`).
- `CheckoutAuthCard` (`src/components/storefront/`): when `useSession()` has no session, renders the Guest/Log In/Register tabs (Contact Details lives in the parent and is never touched by tab switching, satisfying the confirmed decision). When a session exists, the tabs disappear entirely in favor of a "Logged in as {email}" line plus "Use this" chips for every saved address/phone (fetched client-side from `customer_addresses`/`customer_phones`, RLS-scoped to the caller automatically). Login/Register here call the same `supabase.auth` methods as the standalone `/login`/`/register` pages; Register handles both the confirmations-on and confirmations-off cases the same way `/register` does.
  - **Deviation from the Figma annotation**: the inline Register tab includes First/Last Name (the annotation described only Email/Password/Confirm Password) — Contact Details right below already asks for a name, but that's the _delivery contact_, not necessarily the account holder (e.g. ordering for someone else), so the two were kept independent rather than having Register silently borrow from Contact Details.
- **Promo Code**: real lookup against `promo_codes` (`active = true`, unexpired, `min_order_amount` checked against the live cart subtotal), not a mock. No promo codes are seeded yet, so there's nothing to test against until the owner or a migration adds one.
- **Save Address**: writes one `customer_addresses` row (using the translated word "Address" as the label — Checkout's Contact Details has no dedicated label field the way Register/Profile do) and, if Phone Number 1 is filled, one `customer_phones` row. Surfaces the 5-row cap trigger's error as a plain "you've reached the limit" message rather than a raw Postgres error.
- **Scope boundary — deliberately not built yet**: actual order submission (`orders`/`order_items` inserts for both guest and account paths) is a separate TASKS.md item. "Place Order" is present but disabled with a caption, same pattern Cart's own checkout button used pre-Phase-4. Contact Details has no field-level validation yet — nothing to validate against until submission exists; lands together with that task rather than being built and then rebuilt.

### Order Confirmation page

- Success block: check icon, "Order Placed!", a greeting line ("Thanks, {first name} — we've got your order."), and the order number (format `YC-YYYYMMDD-####`, e.g. `YC-20260822-0842`).
- "What Happens Next" card: explains the team will follow up over WhatsApp/phone to confirm final price, plus a "Message Us on WhatsApp" button — **confirmed via annotation**: this is a plain `wa.me` deep link prefilled with **the order number only** (no customer name or item summary — owner looks those up in the admin dashboard), not any WhatsApp API integration (matches the locked stack decision).
- "Order Details" card: line items (thumbnail, name, an attribute summary string like `24>30 • 1 Tier • Vanilla, Chocolate • Black, Red • Heart • Qty 1`, price), a FULFILLMENT section (method + area, date), a CONTACT & DELIVERY ADDRESS section (name, phone + contact-method label, address), and the total with the same WhatsApp-confirmation-pricing disclaimer used elsewhere.
- "Continue Shopping" CTA at the bottom, links back to the storefront.

**Implemented** (`src/app/[locale]/(storefront)/order-confirmation/page.tsx`) — reads a one-time `OrderConfirmationSnapshot` from `sessionStorage` (`src/lib/orders/lastOrder.ts`), never the DB (see "Order creation" below for why). Redirects home if visited directly with no snapshot present (e.g. a bookmark, or the tab was closed and reopened). The check-circle icon uses lucide's `CircleCheck`, not a hand-authored SVG — the Figma asset URL was a temporary export link, not something to hotlink or recreate by hand.

## Order creation (Phase 4)

- **`create_order` Postgres RPC** (`20260815150100_create_order_rpc.sql`, `security definer`, called from `src/lib/orders/createOrder.ts`): the actual write path for both guest and account orders. Wraps `orders` + `order_items` + `order_item_flavors` + `order_item_colors` + `promo_code_redemptions` in one atomic transaction — chosen over five sequential client-side `.insert()` calls specifically because a mid-checkout failure on call #3 would otherwise leave a broken, half-written order sitting in the table for an admin to eventually notice and have to clean up by hand. The function re-validates the two things RLS would otherwise enforce (a logged-in caller can only create an order for `auth.uid()`; a guest order must carry `guest_name`/`guest_phone`) since it runs as `security definer` and bypasses the underlying tables' RLS insert policies (which are left in place, unused today, as a safety net for any future direct-write path). `p_items` is a `jsonb` array mirroring `CartItem` (`src/types/cart.ts`) 1:1, including nested `flavor_ids`/`color_ids` for the join tables.
- **`orders.order_number`** (`20260815150000_order_number.sql`): format `YC-YYYYMMDD-####`, generated by a `before insert` trigger reading a real Postgres sequence (`orders_order_number_seq`) — not a same-day `count(*)`, which would race under concurrent checkouts.
- **Guest order visibility — confirmed, not changed**: the Roles section above already says guests get "a UUID-linked order visible only to admin," meaning there's intentionally no RLS SELECT policy letting a guest re-fetch their own order afterward (`orders_select_own` requires `auth.uid() = customer_id`, which is never true for a guest). Order Confirmation therefore never queries the database — Checkout builds the confirmation snapshot from data it already has at the moment of successful submission and hands it off client-side. This applies uniformly to logged-in customers too (one code path, not two), even though they technically _could_ be read back from the DB — that capability is reserved for the future Profile/Order History page instead, which has an actual reason to re-fetch.
- **Bug fix surfaced while wiring this up**: Checkout's Promo Code "Apply" (built in the previous session) queried `promo_codes` directly via `.select()`. That table's public SELECT policy was dropped in an earlier migration (`20260814090200_promo_code_lookup.sql`, predates this session) in favor of a narrow `validate_promo_code(p_code)` RPC — direct SELECT would have let anyone enumerate every active code's discount terms. Missed when Checkout was first built (the older migration wasn't cross-checked at the time); fixed to call the RPC instead.
- **Checkout's Contact Details now has real required-field validation** (First/Last Name, Address, Phone 1 — scroll-to-first-error + inline red border/message, matching the `CakeCustomizer`/`Register` convention), added together with submission since there was nothing to validate against before "Place Order" actually did something.
- Cart is cleared (`useCartStore.getState().clear()`) immediately after a successful order, before navigating to Order Confirmation.

### Register page

- Only **First Name, Last Name, Email, Password** are required (standalone `/register` page — no Confirm Password field here, per the actual Figma frame).
- Address and phone number sections are **collapsed by default** (just a "+ Add Another Address (1 of 5)" / "+ Add Another Phone Number (1 of 5)" button, no entry form shown) — clicking expands an entry with Remove; the "+ Add" button's counter increments and the button disappears once 5 is reached. Fully optional to skip both; addable later from Checkout or the Profile page.
- Each phone number entry pairs with the same Call/WhatsApp/Both contact-method chips used elsewhere.
- **Confirmed (resolves the Figma frame inconsistency)**: **Confirm Password** is added to every password-creation form — the standalone `/register` page (beyond what its current frame shows) and the inline Checkout Register tab both get it, for consistency rather than following either frame literally.
- **All password fields** (Login, Register, inline Checkout Register tab) get a show/hide toggle (eye icon) — **hidden by default**. Not in the current Figma frames; a UX addition confirmed at the start of Phase 4 implementation.

### Login page

- Standard Email/Password with a Forgot Password link — **not yet wired to a flow, open item** for a later phase.
- Password field gets the same show/hide (eye icon, hidden by default) toggle as Register/Checkout.

### Profile page (`/profile`, new scope)

- Sidebar: editable Profile Info card (name/email + Edit button), Saved Addresses card (each entry has a short **label** like "Home"/"Work" plus the address text, Edit/Remove, "+ Add Address (N of 5)" counter button), Saved Phone Numbers card (same pattern — number + contact-method label like "WhatsApp", Edit/Remove, up to 5).
- Main panel: Order History — cards per order (order number, date, color-coded status badge [Completed=green, Confirmed=blue, Cancelled=red, Pending presumably a 4th color — not shown in the 3 example cards], one-line item summary, price, "View Details" link).

**Implemented** (`src/app/[locale]/(storefront)/profile/page.tsx` + `ProfileInfoCard`/`SavedAddressesCard`/`SavedPhonesCard`/`OrderHistoryList`) — requires a session (redirects to `/login` if none), everything reads/writes live from the DB since a logged-in customer's own data is a normal RLS-scoped read (`profiles_update_own`, and the owner-only policies on `customer_addresses`/`customer_phones`/`orders`), unlike guest orders (see "Order creation" above).

- Email is shown read-only, not editable here — changing it would need Supabase's own re-confirmation flow (a new verification email to the _new_ address), which is its own scope beyond a simple field edit. First/Last Name are editable inline (Edit → inline fields → Save, writes `profiles`).
- **Change Password** (added after feedback): inside the same Profile Info edit form, an optional "Change Password" section (Current/New/Confirm New Password, all-or-nothing — leave all three blank to just save the name). Supabase has no standalone "verify current password" call, so `handleSave` re-authenticates via `supabase.auth.signInWithPassword({ email, password: currentPassword })` first (confirms the caller actually knows it, refreshes the session as a side effect) before `supabase.auth.updateUser({ password: newPassword })` is allowed to run.
- Saved Addresses/Phones: Edit turns that one entry into an inline form (reusing the same label/address/apartment or phone/contact-method fields as Register). Edit/Remove use the real `Button` component (`brand-ghost`/`destructive` variants — matching Profile Info's own Edit button, not a plain text link). Remove confirms via the shared `ConfirmDialog` (`src/components/ui/confirm-dialog.tsx`, Base UI `Dialog`) — **not** `window.confirm()`, replaced after feedback that a native browser confirm looked out of place; this component is meant to be the one used for any future confirm-before-destructive-action anywhere in the app, not just here. The 5-row cap trigger's error surfaces as a plain "you've reached the maximum" message.
- Order History pulls `orders` filtered to the caller (`customer_id = auth.uid()`, the same `orders_select_own` policy mentioned in "Order creation" above) with a nested `order_items(quantity, cakes(name))` select for the one-line item summary. **Known limitation**: `cakes`' RLS only exposes `active = true` rows to a plain customer (unlike the accountant role, which the schema deliberately lets resolve historical/disabled items) — a past order referencing a since-discontinued cake would show a blank name here. Not fixed this pass; would need either exposing inactive cakes to a customer for their own past orders, or denormalizing the cake name onto `order_items` at creation time.
- **"View Details" (`OrderDetailModal`)**: originally skipped as out of scope, then built after feedback that the compact card alone wasn't enough — and expanded a second time after feedback that it needed the cake photo and full customization detail, not just fulfillment/totals. Each line item now shows the cake photo (`cakes.primary_image_url`, `next/image`) and an expandable "Cake Details" section (size or fake-size-cm, tier count, flavors, icing colors + arrangement notes, shape, topper, text on cake/board, notes, and the reference image if one was uploaded). The reference image now renders via `next/image` (switched from a plain `<img>` once `reference_image_url` became a real Cloudinary URL — see "Customer-facing reference-image upload" below); orders placed before that change may still have a dead `blob:` URL stored, which is expected (no real pre-launch order data to migrate).
  - The expanded query embeds `shapes` twice (real `shape_id` and `fake_shape_id`, both FK to the same table) — disambiguated via PostgREST's relationship-hint syntax (`shape:shapes!order_items_shape_id_fkey(name)`), relying on Postgres's default `<table>_<column>_fkey` constraint naming since none of the relevant migrations named these constraints explicitly. Not verified against the live hosted schema this session (Supabase MCP wasn't authenticated) — if "View Details" ever throws a PostgREST relationship error, this hint is the first place to check.

### Checkout auto-fills from the account when logged in

Contact Details' First Name, Last Name, and Email pre-fill from `profiles`/`session.user.email` once a session is available — but only into fields still empty, same "never overwrite what's typed" rule as the saved-address "Use this" chips (confirmed decision from earlier in Phase 4).

### Bug fixed: order placed but Confirmation page never showed

`handlePlaceOrder` used to call `useCartStore.getState().clear()` immediately before `router.push("/order-confirmation")`, both on `/checkout`. Clearing the cart while still mounted there made `items.length === 0` true on the next render, which fired Checkout's own "redirect to `/cart` if empty" effect — racing (and sometimes winning) against the navigation to Order Confirmation, so the customer could land back on an empty cart instead, even though the order itself had already been created successfully (visible afterward in Profile's Order History). Fixed by moving the cart-clear into Order Confirmation's own effect, after it has already read the snapshot — Checkout no longer touches the cart at all on success.

### Bug fixed: Order Confirmation showed briefly then redirected home

A second, related bug: even after the fix above, the page could still redirect to `/` a couple of seconds after appearing. Root cause: the mount effect's dependency array was `[router]`, and if that reference isn't stable across renders (or the `setOrder()` call inside the effect itself triggers a re-render), the effect body can run a second time — finding `sessionStorage` already cleared from the first run, and redirecting home as if no order existed. Fixed with a `useRef` guard so the effect's actual logic only ever executes once, regardless of how many times React invokes the effect callback.

### Home page LCP warning

Next.js's dev-mode console flagged `birthday.jpeg` as the Largest Contentful Paint candidate without `loading="eager"`. The Shop-by-Category grid already passes `priority` to its first three `CategoryCard`s (birthday included), but the same file is _also_ reused as the placeholder photo for Birthday's Trending Cakes row further down the same page (`ProductCard`, Phase 3's placeholder-cake seed) — those instances had no `priority` at all. Fixed by passing `priority` to the first category's first three `ProductCard`s too, matching the Shop grid's existing convention.

### Add to Cart no longer auto-navigates to Cart

`CakeCustomizer`'s "Add to Cart" used to call `router.push("/cart")` immediately after adding the item — changed (owner decision) to a confirmation modal instead (Base UI `Dialog`, fully controlled, no `Dialog.Trigger`) with **Continue Shopping** (just closes the modal — the customer stays on the same Cake Detail page, useful for adding several items from one product) and **Go to Cart** (navigates to `/cart`, matching the same `Button render={<Link .../>}` pattern used elsewhere for link-styled buttons).

### Cart items are now editable

Each `CartItemRow` gained an Edit button (pencil icon, next to Remove) alongside the existing quantity stepper. Clicking it stashes the full `CartItem` in `sessionStorage` (`src/lib/cart/editItem.ts`, `setEditCartItem`) and navigates to that cake's Detail page. `CakeCustomizer` reads it once on mount (an effect, not a lazy `useState` initializer — same SSR/hydration-mismatch reasoning as Order Confirmation's snapshot read) and pre-fills every field from it, then clears the sessionStorage key.

- **Reversed (2026-08-18, owner decision — supersedes the "never touches the original row" call above)**: editing now updates the original cart row in place. `getEditCartItem()`'s pre-fill effect also captures the stashed item's `id` into `editingItemId` state; when set, the submit button reads **Save Changes** (`saveChanges`/`saveChangesWithPrice`, `CakeDetail` namespace) instead of Add to Cart, reuses that same `id` when building the new `CartItem`, calls the cart store's new `updateItem(id, item)` (replaces the row at that id — `src/store/cart.ts`) instead of `addItem`, and navigates straight to `/cart` instead of opening the "Added to Cart" confirmation modal (which stays as-is for the non-editing add-to-cart path).
- **Estimated-price labeling (2026-08-18, owner request)**: since no price is final until the business confirms it over WhatsApp/phone (see Checkout's `priceDisclaimer`), a small `Common.estimatedPriceLabel` ("Estimated price") now sits next to the price on Cake Detail (`CakeCustomizer`, whenever `unitTotal > 0` — not shown for "Price on request" items with no base price) and under each line price in `CartItemRow`.
- **Fixed** (was: "known limitation carried over from Fake Cake's reference image" — `referenceImageUrl` used to be a client-side `blob:` URL that only resolved while the tab that created it stayed open): reference images are now uploaded to Cloudinary immediately on file selection, so `referenceImageUrl` in a re-edited/persisted cart item is a real, permanent URL — see "Customer-facing reference-image upload" below.

### "Use this" chips and Order Details' close button

Two smaller polish items from feedback:

- Checkout's saved-address/phone "Use this" chips were plain underlined text links — now a real `Button` (`variant="brand-primary"`), matching the styling of the Apply button next to Promo Code, for visual consistency between the two "commit a suggested value" actions on the same page.
- `OrderDetailModal`'s close (X) button is now `sticky` within the popup's own scroll container (bleeds to the popup's edges with a matching background so content scrolls invisibly underneath it) instead of scrolling away with the content, and slightly larger (`size-10` button / `size-5` icon, up from `size-8`/`size-4`).

### Data model additions needed for Phase 4 (saved addresses/phones)

**Implemented** (`20260815130000_customer_addresses_phones.sql`, pushed to hosted): dedicated join tables (`customer_addresses`, `customer_phones`), not JSONB arrays — matches the existing `order_item_flavors`/`order_item_colors` join-table pattern and keeps RLS/edit/remove straightforward. Confirmed requirements driving the design:

- Saved **customer addresses**: capped at 5 per customer, each with a short free-text **label** (customer-typed, e.g. "Home"/"Work"/"Mom's House" — not a fixed enum) in addition to the address text (street/apartment).
- Saved **customer phone numbers**: capped at 5 per customer, each with a call/WhatsApp/both contact-method preference.
- Both need to be selectable/auto-fillable from Checkout (as chips) and manageable (add/edit/remove) from the Profile page and from Register's optional expandable sections.
- Cap enforcement: a shared `fn_enforce_customer_item_cap()` trigger function (reads the max via `TG_ARGV[0]`, one function serves both tables). RLS: owner-only CRUD (`(select auth.uid()) = customer_id`, scoped to `authenticated` — guests can't have saved rows) plus `admin_all`; **no accountant policy** (address book isn't accountant's domain, see Roles) — **flagged again in Phase 7 below**, since the new Customers List admin page wants to show this data and Accountant can otherwise reach Analytics. Audit-logged via the standard `fn_audit_log()` triggers, same as every other table.

## Phase 5 — Admin: catalog

Admin dashboard (`src/app/[locale]/(admin)/admin/...`, mounted at `/admin`) with a collapsible sidebar (`AdminSidebarNav`/`AdminNavItem`) grouping Catalog (active: Cakes, Categories, Sizes, Flavors, Colors, Toppers) alongside disabled placeholders for Orders/Money/Team, reserved for Phases 6-8.

- **Auth guard**: `src/lib/admin/requireAdmin.ts` gates every `/admin` route server-side to `role === 'admin'` only — the accountant role (see Roles above) has no catalog access, matching the original design. There's no admin-invite UI yet, so the first admin account has to be granted by hand via a direct `profiles.role` update in the Supabase SQL editor; building that UI is Phase 8 territory.
- **`cake_images.is_primary`** + `fn_sync_cake_primary_image()` trigger + `toppers.image_url` (`20260815160000_phase5_admin_catalog.sql`): the admin marks one uploaded image per cake as primary, and the trigger keeps `cakes.primary_image_url` in sync so every existing storefront read (`ProductCard`, Home, Cart, `CakeCustomizer`, `OrderDetailModal`) needed zero changes. `toppers.image_url` closes a pre-existing gap where `TopperCard` had an `imageSrc` prop nothing ever populated — toppers now show a real photo in the storefront customizer once an admin uploads one.
- **Cloudinary uploads are signed, not unsigned**: `src/app/api/admin/cloudinary-signature/route.ts` checks the caller's session and admin role server-side, then computes the signature with Node's built-in `crypto` — no `cloudinary` npm package was added. The browser uploads directly to Cloudinary with that signature via `src/lib/admin/cloudinaryUpload.ts` / `ImageUploader`. Used by the Cake Form (multi-image, `multiple={true}`, with primary-marking) and the Toppers page (single image).
- **Shared vs. bespoke table UI**: `AdminTable`/`RowActions` (`src/components/admin/AdminTable.tsx`, `RowActions.tsx`), plus `Select`/`Switch` (`src/components/ui/select.tsx`/`switch.tsx`, `@base-ui/react`-backed like `Button` — not Radix, so no new heavy dependency), drive Colors/Flavors/Toppers/Sizes, which are shaped similarly enough to share one implementation. Cakes and Categories have bespoke layouts instead (filter chips + sortable columns + pagination; native drag-reorder + nested subcategories) since they diverge too much from that shape. `AdminTable`/`RowActions` were extended later (Cakes list) with additive optional props only (`buildSortHref` replacing the older `sortBasePath`/`sortKey`/`sortDir`; `RowActions` gained an optional `editHref` for navigation-based edit) — verified not to affect the Colors/Flavors/Toppers/Sizes pages built against the earlier shape.
- **Error-handling + `sort_order` pattern**, established on Colors after code review caught two real bugs in the first draft, then applied to every later CRUD page (Flavors, Toppers, Sizes, Categories, Cakes, Cake Form): every Supabase write checks `{ error }`, surfaces it via an `error: string | null` state rendered inline (reusing the `Admin.table.saveFailed` translation key), and never mutates local state or proceeds past a failed write. New `sort_order` values on insert are computed as `Math.max(...existing) + 1`, never a plain `.length` — after a delete leaves the array non-contiguous, `.length` could collide with an existing row's `sort_order`.
- **Categories page**: native HTML5 drag-and-drop, no drag-and-drop library — Candy Corner's 4 subcategories nest under it with independent reorder. A Critical bug found and fixed during review: dragging a row across groups (top-level ↔ a Candy Corner subcategory) could silently corrupt an unrelated row's `sort_order`, because `Array.prototype.splice(-1, 1)` was reached when the dragged item wasn't actually a member of the drop target's group (an `indexOf` returning `-1` treated as "last element" instead of "not found") — fixed with an explicit `fromIndex === -1` guard.
- **Cake Form** uses the real shadcn `Select` component for Category/Subcategory (not a native `<select>`), per the design spec's explicit requirement, plus the shared `ImageUploader` in multi-image mode for photos.
- **Follow-up pass, closed**: `Pagination` gained an optional `extraParams` prop (storefront callers unaffected) so the Cakes admin list's filter/sort survive pagination. Toppers' `topper_colors` write now goes through `fn_replace_topper_colors` (`20260816110000_replace_topper_colors_rpc.sql`), a single `security definer` RPC, instead of a non-transactional delete-then-insert.

### Figma design-alignment pass (post-launch of the above)

The original Phase 5 build above was implemented without actually consulting the Figma file — corrected in a follow-up pass this session, pulling real `get_design_context`/screenshots and reconciling. Notable changes beyond visual polish:

- **`cakes.allow_fake`** (`20260816100000_cakes_allow_fake.sql`): a per-cake toggle surfaced in the Figma Cake Form ("Available as Fake Cake") with no prior schema backing. Storefront eligibility (`src/app/[locale]/(storefront)/cakes/[id]/page.tsx`) ANDs it with the existing category-based Bento/Candy-Corner exclusion.
- **Categories now have real images**: `categories.image_url`/`image_public_id` (`20260816120000_cloudinary_public_ids.sql`), editable via a new `ImageUploader` in `CategoryFormDialog`. `CategoryCard` reads `category.image_url` directly and renders nothing (not a broken image) when null.
- **Placeholder photos removed from the live site, not migrated** (owner decision): `20260816130000_remove_placeholder_images.sql` nulls out the 24 seeded cakes' `primary_image_url`/`cake_images` rows and deletes the local `/images/categories/*.jpeg` references from code — real photos get uploaded through the now-working admin UI instead of an automated migration of placeholders.
- **Cloudinary asset cleanup**: uploads now carry Cloudinary's `public_id` alongside the URL (`cake_images.public_id`, `toppers.image_public_id`, `categories.image_public_id`). Removing an image from a cake/topper/category, or deleting the row itself, calls a new signed `/api/admin/cloudinary-delete` route so orphaned assets don't pile up in Cloudinary storage — previously only the DB reference was removed.
- **Fixed a real pre-existing bug**: `getTrendingCakesByCategory()` never filtered on `cakes.featured` — Home's "Trending Cakes" showed the first 4 _active_ cakes per category regardless of the toggle (harmless only by coincidence, since every seeded placeholder cake already had `featured = true`). Now correctly filtered, plus a new `/admin/cakes/trending` page to manage which cakes are featured and their order per category.
- **`next.config.ts` was missing `images.remotePatterns`** for `res.cloudinary.com` entirely — a gap dating back to Cloudinary's original wiring in Task 4, never caught since nothing had been live-tested against a real Cloudinary URL until this session. `next/image` would have rejected every Cloudinary photo outright.
- **Categories generalized beyond Candy Corner**: any top-level category can now have subcategories (an "+Add Subcategory" affordance sits under every category, not just an expanded Candy Corner group), with reorder using the View Transitions API (`document.startViewTransition`, native browser API, no new dependency) for a smooth animated reorder instead of an instant snap.
- **Flavors' category restrictions are now editable** (not just displayed) — `FlavorFormDialog` gained a category picker writing `category_flavors` directly.
- Sidebar gained a real per-admin name (from `profiles.first_name`/`last_name`, threaded through `requireAdmin`) and an EN/AR language toggle in the footer (not in Figma, added per owner request, reusing the same `next-intl` locale-swap `Link` pattern as the storefront `NavBar`).

### UX polish pass (post-Figma-alignment)

- **`getTopLevelCategories()` had a hardcoded slug allowlist** (`TOP_LEVEL_SLUGS`) restricting Home/Shop to only the 6 originally-seeded categories — any category the admin added afterward would never appear on the storefront regardless of any other setting. Removed; the query now returns every active top-level category, matching what the admin panel already let you create.
- **Admin sidebar rebuilt to avoid a `fixed`+`transform`+RTL-variant combination merged through `tailwind-merge`** that was suspected of causing "sidebar doesn't render on desktop" / "stuck after zoom" reports. Desktop and mobile now render via two structurally separate paths (`hidden lg:flex` static desktop sidebar with no transform at all; mobile drawer only mounted in the DOM while open) rather than one component juggling both states through merged Tailwind classes. Also gained a "go to home page" icon button next to the brand text.
- **Cakes admin search fixed**: the page's `key` prop included the `search` param, so every debounced keystroke's URL update forced a full remount of `CakesListContent` — dropping input focus mid-type. Replaced the remount-on-every-filter-change pattern with adjusting `rows` state during render when the `cakes` prop changes (React's documented pattern for this, avoids the `set-state-in-effect` lint rule too) — removes the need for `key` entirely.
- **Custom `ColorPicker`** (`src/components/admin/ColorPicker.tsx`, hue slider + saturation/value square, pointer-event math, no canvas) replaces the native `<input type="color">` popup, whose position is OS/browser chrome and can't be centered via CSS — flagged as a build-vs-dependency tradeoff, built custom rather than adding a picker library.
- **Storefront NavBar** gets an admin-only "Dashboard" link in the Profile dropdown; `useSession()` now also returns `role`/`isAdmin` (a `profiles.role` lookup alongside the auth session).
- **Shop gained subcategory filter chips** (`ShopBrowse`, mirroring the admin Cakes list's pattern) — previously subcategory filtering existed only in the admin, not on the storefront.
- **Trending Cakes carousel** (`src/components/storefront/TrendingCarousel.tsx`): replaces the plain `overflow-x-auto` scroll row with a slow infinite auto-loop (duplicated content + wraparound `scrollLeft`, `requestAnimationFrame`), left/right arrow nudges, native touch-scroll on mobile (no extra gesture library), and a hidden scrollbar. Pauses on hover/after a manual nudge.
- **Per-category trending control**: `categories.show_trending`/`trending_sort_order` (`20260816140000_categories_trending.sql`, top-level categories only, independent of the `sort_order` that drives the Shop grid) — managed from `/admin/cakes/trending`, which now also has category-level show/hide and reorder alongside the existing per-cake management.
- Cake Detail's reference-image upload box used `bg-bg-page` (page background), making it nearly invisible against the page itself — changed to `bg-bg-surface`, matching the off-white other buttons/chips use. Home's hero subtext lost its semi-transparent background chip per owner request (plain text now).

### Second UX follow-up round

- **Reference-image box, corrected**: the _empty_ (pre-upload) state needs `border-white`/`bg-bg-surface` so the dashed clickable area reads clearly against the page; the _filled_ (post-upload) state was reverted back to `bg-bg-page` so the letterboxed space around a non-square photo blends with the page instead of standing out as its own surface — these are two different, opposite-direction fixes to the same component from the previous round's single blanket change.
- **Trending carousel animation bug**: the track had `scroll-smooth` applied, which makes the browser animate _every_ programmatic `scrollLeft` write — including the per-frame `requestAnimationFrame` increments — fighting the manual animation and making it look frozen/broken. Removed; only the arrow-nudge's explicit `scrollTo({ behavior: "smooth" })` call still animates. The wrap-around math (both the rAF tick and the nudge handler) switched from a single conditional subtract/add to `% halfWidth`, so it can't drift or "reset after one loop" regardless of how many times it wraps. Added an `IntersectionObserver` so the auto-scroll only runs while the carousel is actually on screen.
- **Admin sidebar collapsed-state overlap**: the header's Home-button + collapse-toggle-button pair didn't fit side by side in the 76px collapsed rail width; when collapsed (desktop only) the header switches to a vertical stack (`flex-col`) instead of a row.
- **Admin table "Actions" column**: `AdminTableColumn` gained an `align?: "start" | "end"` field; Sizes/Cakes/Flavors/Colors/Toppers now pass `header: t("actions"), align: "end"` on their actions column (previously an unlabeled, left-aligned column) — `RowActions`'s root `div` switched from `flex` to `inline-flex` so the table cell's `text-end` can actually position it.
- **Categories table header**: added an "Active" label above the per-row `Switch` (previously an empty header cell) and centered the subcategory-count column, matching the visual weight of the other admin tables.
- **Category delete now distinguishes the FK-restrict case**: `cakes.category_id` has no `ON DELETE` clause (default `RESTRICT`), so deleting a category that still has cakes assigned to it was surfacing the generic `saveFailed` message. `CategoriesPageContent.handleDelete` now checks the Postgres error code (`23503`) and shows a specific "can't delete — cakes still assigned" message instead. **Reused directly in Phase 7's Manage Categories modal for expense categories — same error-code check, same message pattern.**
- **Profile menu no longer opens on hover**: the previous `onMouseEnter`/`onMouseLeave` handlers plus a manual controlled-`open` state on the storefront NavBar's `Menu.Root` were removed — Base UI's `Menu.Trigger` already opens/closes on click by default, so the menu now only opens on press. Also dropped the trigger's `hover:scale-105` visual "expand".

### Third UX follow-up round

- **`AdminTable`'s "Actions" header still wasn't right-aligned after the previous fix** — the real cause: the header text is wrapped in a `<span className="flex ...">` inside the `<th>`. `display:flex` makes that span a block-level box, which fills the full `<th>` width (block boxes stretch to their container by default) and positions its content via `justify-content`, not `text-align` — so the `<th>`'s `text-align: end` was always being ignored for header cells. Fixed by adding `justify-end` to the span itself when `col.align === "end"`, alongside the existing `text-end` on `<th>`/`<td>`. This is the fix that actually lines "Actions" up above the edit/delete buttons across Sizes/Cakes/Flavors/Colors/Toppers.
- **Categories page (custom flex-row layout, not `<table>`) had a related-but-different problem**: the header row's "Active"/"Actions" label columns were plain fixed-width `<span>`s, but the corresponding row content (the `Switch`, `RowActions`) had no matching fixed-width wrapper — so their actual rendered width (32px for the switch vs. a 56px label column) didn't match the header's assumed width, throwing off alignment. Fixed by wrapping both `Switch` and `RowActions` in `CategoryRow` with fixed-width (`w-10`/`w-[68px]`) `flex justify-end` spans identical to the header's column widths, guaranteeing pixel-exact alignment by construction rather than by coincidental padding math. Subcategory-count column also got a matching `pe-8` on both header and row to shift its centered text left within its slot, per owner feedback.
- **Trending carousel animation removed entirely** — the auto-scroll/infinite-loop treatment (`requestAnimationFrame`, duplicated cake list, `IntersectionObserver` visibility gating) wasn't rendering as animating on the owner's laptop and was scrapped rather than debugged further. `TrendingCarousel` is back to a plain bounded horizontal scroller: cakes render once (no duplication), left/right arrows nudge by a fixed pixel amount (`scrollBy` with `smooth` behavior), native touch/trackpad scroll works via `overflow-x-auto` with the scrollbar hidden, and scrolling simply stops at the first/last cake — no wraparound, no auto-play.

### Fourth UX follow-up round

- **Trending carousel gained scroll-snap** (`snap-x snap-mandatory` on the track, `snap-start` on `ProductCard`) so a card can never sit half-cut at the visible edge after scrolling/nudging — the browser always settles on a full card boundary. The arrow nudge distance (`NUDGE_PX`) changed from an arbitrary `360` to `364` (exactly one `ProductCard`'s `340px` width + the track's `gap-6`/`24px`), so an arrow press always steps exactly one card, landing precisely on the next snap point instead of an amount that happened to be close.
- **Each trending category now sits inside its own bordered card** (`rounded-[32px] border border-border-default bg-bg-surface-alt/60 p-5 sm:p-8` wrapping the heading + carousel on Home) instead of floating directly on the page background — owner feedback that the rows looked disconnected from the page.
- `ProductCard` gained `snap-start` unconditionally (safe no-op anywhere it's used outside a `snap-x` container, e.g. Shop's flex-wrap grid) rather than adding a variant prop, since every current usage benefits from it.

### Customer-facing reference-image upload (Cloudinary)

The reference-image field (Cake Detail, every order item — see Core Entities above) used to store `URL.createObjectURL(file)`, a client-side `blob:` URL that only resolves inside the browser tab that created it — dead as soon as that tab closes, meaning it could never actually reach an order placed on a different day, a different device, or be viewable by the admin at all. Fixed by wiring up a real, customer-facing Cloudinary upload path:

- **Uploads immediately on file selection**, not deferred to "Add to Cart" or order submission. Reasoning: the cart is `localStorage`-persisted (`src/store/cart.ts`) and can survive days/browser restarts, but a `blob:` URL can't — so the upload has to happen before anything gets persisted, and uploading at selection time (matching the existing preview moment) is simpler than adding a separate upload-on-submit step, and means Cart/Checkout/Order Confirmation/admin Order Details all just read one already-valid URL with no special-casing. "Add to Cart" is blocked (with a scroll-to-and-highlight, matching every other required-field validation) while an upload is still in flight, so a half-finished upload can never get submitted.
- **No auth required, since guests can order without a session** — this can't reuse the admin's `/api/admin/cloudinary-signature` route (session + `role = 'admin'` gated). New route: `src/app/api/reference-image/cloudinary-signature/route.ts`, folder-scoped to `reference-images` (separate from every admin-managed folder) and format-restricted (`allowed_formats=jpg,jpeg,png,webp,heic,heif`, a signed param so it's actually enforced server-side, not just a client-side accept hint).
- **Abuse/spam is bounded by a per-IP rate limit**, not a new external service (Redis/Upstash/etc. would need a new dependency flagged under hard rule 8 — a Postgres-based limiter reuses the stack already in place). `reference_image_upload_log` (ip_address, created_at) + `check_reference_image_upload_rate_limit(p_ip)` (`security definer` RPC, `20260816150000_reference_image_rate_limit.sql`) atomically checks-and-records one attempt per call, capping at 8 uploads per IP per 10 minutes, and opportunistically prunes rows older than a day on every call (no cron job exists in this project to do it separately — see the flagged follow-up below). The signature route calls this RPC before signing anything; a `false` result returns `429`. Client-side (`src/lib/customer/cloudinaryUpload.ts`) also rejects non-image files and anything over 8MB before even requesting a signature, to cut down on wasted round-trips.
- **`order_items.reference_image_public_id`** (`20260816160000_reference_image_public_id.sql`, threaded through `create_order`'s `p_items` jsonb same as every other field) stores the Cloudinary `public_id` alongside the URL, matching the `cake_images.public_id`/`categories.image_public_id`/`toppers.image_public_id` convention — added specifically so a future cleanup job can call Cloudinary's destroy API directly by ID instead of parsing it back out of the URL.
- **`OrderDetailModal`'s reference-image render switched from a plain `<img>` to `next/image`**, now that the URL is a real, optimizable Cloudinary URL instead of a `blob:` one.

**Flagged, explicitly not built this pass (owner asked this be written down rather than done now)**:

- **No scheduled deletion/cleanup job yet.** A customer can upload a reference image and never complete checkout, or complete checkout and have the order sit for years — nothing currently removes orphaned or stale Cloudinary assets in the `reference-images` folder. `reference_image_public_id` (above) is stored specifically to make this easy to build later: a job could delete anything in that folder older than N days with no matching `order_items.reference_image_public_id`, or older than N days past order completion. This project has no cron/scheduled-job infrastructure at all yet (Vercel Cron is the obvious candidate, but that's a new architectural piece — flagging per hard rule 8 rather than adding it silently).
- **Rate limiting is a first pass, not load-tested or owner-tuned.** 8 uploads/10 minutes per IP is a reasonable-guess default, not a number derived from real traffic — revisit once there's actual usage data, and consider whether shared-IP scenarios (e.g. many customers behind one NAT/office network) need a higher ceiling or a different limiting key.

### Admin UX/bugfix pass (post-Figma-alignment)

- **`AdminShell`** (`src/components/admin/AdminShell.tsx`) now wraps `AdminSidebarNav` — the admin layout previously just placed the sidebar and content in a static flex row, which meant the full-width (240px) sidebar was always present and crushed content on phones. `AdminSidebarNav` is now a fixed off-canvas drawer below the `lg` breakpoint (translate-x hidden by default, opened via a hamburger button in a new mobile top bar, closes on backdrop click or nav-link click); at `lg`+ it behaves exactly as before (static, collapsible icon rail).
- **Post-launch fix (2026-08-18): the desktop sidebar `<aside>` was a stretched flex child, not viewport-height** — with `align-items: stretch` (flex default) it grew to match the main content column's height, so on a long/tall admin page the nav-items/profile/logout/language block at the bottom kept getting pushed further down the page instead of staying pinned near the screen's bottom edge (owner report: "don't extend it to how big the page is"). Fixed by making the desktop `<aside>` `sticky top-0 h-screen` instead of a plain stretched flex item — it's now always exactly viewport-height (ending right after the profile/logout/language row, same as before) and stays in view while the main content column scrolls past it.
- **Real bug fixed**: none of `getTopLevelCategories`/`getCategoryBySlug`/`getSubcategories`/`getCategoryById` (`src/lib/catalog/queries.ts`) ever filtered on `categories.active` — the admin's per-category "Active" toggle (`CategoryRow`) silently did nothing on the storefront (a disabled category still showed in Shop-by-Category, Trending Cakes, and Shop). All four now filter `.eq("active", true)`.
- Admin catalog list/edit page `<h1>`s recolored from `text-text-primary` (near-black, `#2b1e19`) to `text-brand-primary` (`#501907`, already the app's defined brand brown) — owner feedback that catalog page titles read as black.
- **Login now role-routes**: `/login` fetches the signed-in user's `profiles.role` immediately after `signInWithPassword()` and sends admins to `/admin` instead of `/`, customers to `/` as before.
- **Admin home page** (`/admin`) is a real welcome screen — server component, fetches the caller's name and renders "Welcome back, {name}!" — replacing the placeholder "Coming soon" text. **Superseded by the Phase 7 Dashboard-Home page below**, which replaces this plain welcome screen with real stat cards and an upcoming-deliveries list.
- **Cakes list search**: `/admin/cakes` accepts a `search` query param (substring match against both `name.en` and `name.ar`, case-insensitive, applied in-memory alongside the existing category/sort/pagination filtering server-side) with a debounced client-side input (`CakesListContent`) that updates the URL.
- **shadcn `Select` dropdown fixes** (`CakeForm`'s category/subcategory pickers, Sizes' category picker): trigger background changed from `bg-transparent` (blended into the page's cream `bg-page`) to `bg-bg-surface` (white); items given visible separators and taller touch targets (`py-2.5`); `alignItemWithTrigger={false}` passed to `SelectContent` so the popup always opens below the trigger like a normal dropdown, instead of Base UI's default "align the selected item with the trigger" behavior, which could open the list upward off-screen when the last item was selected.
- **Colors' hex picker** (`ColorFormDialog`) now opens a small popover (native `<input type="color">` + hex text field) with explicit Confirm/Cancel buttons — previously the native color input applied every change live with no way to back out.
- **Cake Form**: "Available as Fake Cake" is now force-disabled (and saved as `false`) whenever the selected top-level category is Bento or Candy Corner, matching the storefront's own Bento/Candy-Corner fake-cake exclusion (`allow_fake` was previously always editable regardless of category). Cancel button changed from `brand-secondary` (orange) to a white/`brand-ghost` style. The category-select + price-modifier row was a non-wrapping `flex` row that overflowed on narrow phones — now stacks on mobile (`flex-col sm:flex-row`).

## Phase 6 — Admin: orders & operations

Figma design confirmed for all six Phase 6 screens (Orders list, Order Detail, New Order, Delivery Areas, Delivery Calendar, Promo Codes) — same file, pages `Admin - Orders`, `Admin - Order Detail`, `Admin - New Order`, `Admin - Delivery Areas`, `Admin - Delivery Calendar`, `Admin - Promo Codes`. Implemented screen-by-screen, pausing for review after each.

- **Auth guard widened**: `/admin`'s shared layout previously gated with `requireAdmin` (admin-only), which would have blocked the Accountant role from Orders entirely — but ARCHITECTURE.md's own Roles section already says accountants need read-only order access, and the Order Detail spec explicitly requires it. `src/lib/admin/requireAdmin.ts` now exports `requireStaff` (admin OR accountant) alongside the original `requireAdmin` (admin-only); the layout switched to `requireStaff` so both roles reach `/admin`, and every catalog page (`cakes`, `cakes/[id]`, `cakes/trending`, `categories`, `sizes`, `flavors`, `colors`, `toppers`) now calls `requireAdmin` itself to stay admin-only — the layout alone no longer enforces that. `AdminShell`/`AdminSidebarNav` take a `role` prop: the Catalog and Team nav sections are hidden entirely for accountants, and the Orders section's non-Orders-list links (Delivery Areas/Calendar, Promo Codes — admin-only sub-pages) render dimmed/unclickable for accountants exactly like the existing Money-section placeholders. **Phase 7 reuses `requireStaff` for Dashboard-Home, Analytics, and its drill-down pages; `requireAdmin` (admin-only) for Expenses (both entry and Manage Categories) and the New Order/Order Detail-style privileged actions — see Phase 7 section for the one open exception (Customers List's address/phone panel).**
- **New RLS policy**: accountants already had `accountant_read` on `orders`/`order_items`/`order_item_flavors` (Phase 2), but not on `profiles` — so the Orders list's `orders → profiles` embed for the customer name would have silently come back null for an accountant (PostgREST drops an embedded relation the caller can't read rather than erroring). Added `accountant_read` on `profiles` (`20260816170000_accountant_profiles_read.sql`), same shape as the existing accountant catalog-read policies.
- **Status badge colors**: new `--status-pending`/`--status-confirmed`/`--status-completed`/`--status-cancelled` CSS variables (`#d98e1b`/`#4a6fa5`/`#3e7d4c`/`#c23b2e`) in `globals.css`, exposed as Tailwind `status-*` theme colors so `bg-status-pending/15 text-status-pending` works directly — kept separate from the existing `--destructive` token (used for delete buttons/error text) since the design calls out specific hex values for order statuses that don't match it. `StatusBadge` (`src/components/admin/StatusBadge.tsx`) is the shared component, reused by Order Detail too.
- **Every new admin screen's content area uses `bg-bg-surface-alt`** (`#f6e3d2`) per the design's styling note — this is additive to Phase 5, which keeps its `bg-bg-subtle` background as-is. **Phase 7's Dashboard-Home, Expenses, and Analytics (plus its three drill-down pages) all follow the same convention.**

### Orders list (`/admin/orders`) — done

`src/app/[locale]/(admin)/admin/orders/page.tsx` (server: status/source/date-range filters and sort applied in the Supabase query; search is applied client-side in JS after fetch since it needs hyphen-stripped substring matching, same "fetch filtered set, filter search in JS, paginate in JS" convention as the Cakes list) → `OrdersListContent` (client: search box, status chips, source chips, two independent `DateRangeFilterButton` popovers).

- **Search** strips `-` from both the query and `order_number` before comparing, so `20260814-0921`, `202608140921`, and `YC-20260814-0921` all match.
- **`DateRangeFilterButton`** (`src/components/admin/orders/DateRangeFilterButton.tsx`): a new component, not a reuse of the storefront `DatePicker` — that one is single-date, blocks past dates, and disables `delivery_calendar_blocks` dates, none of which apply here (Order Date is often in the past; admin filtering needs a plain range, no blocking). Click a day then a second day for a range; clicking the same day twice selects a single day; "Any" clears. Centered modal-style popup (backdrop + fixed-centered panel), matching the existing `ColorFormDialog` hex-picker popover pattern rather than a true anchored/floating popover. **Reused by Phase 7's Dashboard-Home and Analytics deep-links** — see Phase 7 section.
- **Default sort**: Order Date, newest first (`created_at desc`) — matches the Figma default and the "sortable, default sort: newest first" spec line.
- **Fulfillment column**: `"{Delivery|Pickup} • {area or fixed pickup location}"` — pickup's location label (`"New Cairo"` / `"التجمع الخامس"`) is a local `PICKUP_LOCATION` constant, matching the same not-centralized duplication already present in `FulfillmentFields.tsx`/`CheckoutPageContent.tsx` rather than introducing a new shared constant this phase didn't ask for.
- **Total column**: `final_price ?? (subtotal_estimate + delivery_price - discount_amount)` — the finalized price once admin sets one, otherwise the live estimate.
- **"+ New Order" button is hidden for the Accountant role** (admin-only privileged write action); the list itself, search, filters, and "View" are all available to both roles.

### Order Detail (`/admin/orders/[id]`) — done

`src/app/[locale]/(admin)/admin/orders/[id]/page.tsx` (server: fetches the order + `order_items` with every catalog join needed to render both the read-only recap and the edit form, keyed per line item's cake category since one order can mix items from different categories) → `OrderDetailContent` (order recap card + Admin Actions card) → `OrderLineItem` per line item (thumbnail/summary row, expandable read-only Cake Details panel, admin-only Edit toggle).

- **`CakeItemFields`** (`src/components/admin/orders/CakeItemFields.tsx`): a new shared, fully-controlled version of `CakeCustomizer`'s field rendering (`src/types/adminCakeItem.ts`'s `CakeItemFieldsValue` instead of `CakeCustomizer`'s internal `useState` + cart submission) — same components (`SelectChip`/`ColorSwatch`/`InputField`/`QuantityStepper`/`TopperCard`/`SizeQuantityInput`), same normal-vs-Fake-Cake branching, reused as-is since they were already pure/controlled. No Cake Type toggle here (unlike the storefront): an order item's `is_fake` is fixed once placed, so the form renders whichever field set already applies rather than letting an admin convert one type into the other. This same component is meant to be reused by New Order's "+ Add customization details" panel (next screen).
- **`update_order_item_customization` RPC** (`20260816190000_update_order_item_customization_rpc.sql`): `security definer`, self-checks `current_profile_role() = 'admin'` (same authorization pattern as `create_order`), and does the `order_items` update plus a delete+reinsert of `order_item_flavors`/`order_item_colors` in one transaction — same reasoning as `fn_replace_topper_colors` (Phase 5): two separate client calls risk a partial write if the second fails. Reference-image swap reuses the existing customer-facing `uploadReferenceImage()` (Cloudinary, rate-limited) rather than adding a separate admin upload path.
- **New `before update` trigger** (`20260816180000_order_status_timestamps.sql`): `orders.confirmed_at`/`completed_at`/`cancelled_at` existed since Phase 2 but nothing had ever populated them (the insert-time trigger only forces them to `null`) — Order Detail's status select is the first place status is ever changed post-creation. `fn_stamp_order_status_timestamps()` sets the relevant timestamp the first time status reaches that value and leaves earlier ones alone (e.g. confirmed → completed keeps `confirmed_at` as a lifecycle record rather than clearing it).
- **Bug found and fixed while building this screen**: `orders.guest_phone` was only ever populated for guest checkouts — `CheckoutPageContent` passed `guestPhone: session ? null : phone1`, so a logged-in customer's typed Contact Details phone was silently discarded at order creation, never stored anywhere (`customer_phones` can hold up to 5 saved numbers with no way to know which, if any, was "the one used here"). Fixed at the schema level, not papered over in the UI: `20260816200000_order_contact_phone.sql` renames `orders.guest_phone` → `orders.contact_phone` and adds `orders.contact_phone_method`, both now populated for **every** order (guest or account) — only the customer's _name_ stays account-derived (`profiles`) rather than duplicated onto the order row. `create_order`'s `p_guest_phone` param renamed to `p_contact_phone`, `p_contact_phone_method` added (defaulted, so the signature change didn't need a function drop). `CheckoutPageContent`/`createOrder.ts` updated to always send `contactPhone`/`contactPhoneMethod` regardless of session.
- **Bug found and fixed**: the Address row was gated on `fulfillment_type === "delivery"`, but `orders.delivery_address` is actually captured for every order regardless of fulfillment method (Checkout's Contact Details "Address" field is always required, not delivery-specific) — pickup orders were silently hiding an address that was right there in the row. Now shown whenever `delivery_address` is non-null.
- **Contact name/address**: name resolves from `profiles.first_name`/`last_name` (customer orders) or `guest_name`; phone resolves from `orders.contact_phone`/`contact_phone_method` (see above); address from `orders.delivery_address`.
- **Phase 7 follow-up (owner request): per-line-item price editing.** `order_items.final_price` (nullable numeric, `20260817100000_order_item_final_price.sql`) — an admin-set override for that one line's total price, same nullable-override convention `orders.final_price` already used (falls back to `line_estimate` when null). `update_order_item_customization` gained a trailing `p_final_price numeric default null` param (appended via `create or replace`, no drop needed — same precedent as the `p_contact_phone` rename above). Order Detail's recap card and Admin Actions panel now show: Items Subtotal (`Σ item.final_price ?? item.line_estimate`), Delivery Fee (`orders.delivery_price`, shown only when `fulfillment_type = 'delivery'` — reuses the value already frozen at checkout time from `delivery_areas.price`, not a fresh lookup, so a historical order stays correct even if that area's price changes later), Discount (shown only if `discount_amount > 0`), and Total (`final_price (defaults to Items Subtotal) + Delivery Fee − Discount`). The order-level Final Price field now pre-fills from the live items subtotal instead of starting blank, but stays a free-typed override.
- **Bug fixed (Phase 7 follow-up)**: neither the order-level Save Changes button nor a line item's Save button surfaced any success feedback — only errors were shown, so a successful save looked indistinguishable from doing nothing. Both now show a brief green confirmation (`orderSaved`/`itemSaved`, auto-clears after 4s via a `setTimeout` effect) alongside the existing error state.
- **Phase 7 follow-up (owner request): delivery fee is now editable too**, not just read from `delivery_areas` at checkout. The Admin Actions panel gets a Delivery Fee input (shown only for delivery orders), pre-filled from `orders.delivery_price` (still the value frozen at checkout), that Save Changes writes straight back to `orders.delivery_price` — covers the "customer is further out than the usual zones, charge more for this one" case without needing a new `delivery_areas` row. Total recomputes live from whatever's currently in that field, same as Final Price.
- **UX fix (owner reported "read-only fields aren't editable" three separate times)**: `OrderLineItem` used to require two clicks — expand (chevron, defaults to a read-only detail view) then a separate "Edit" link to swap into `CakeItemFields`. The owner kept landing on the read-only view via the chevron and concluding editing was broken. Collapsed into one step: for `canEdit` (admin) callers, expanding a line item now goes straight into the editable form (size, tiers, flavors, colors, shape, topper, text, notes, reference image, and the per-item price field, all at once) — no separate edit-mode toggle. Accountants (`canEdit=false`) still only ever see the read-only view, since they have no `order_items` write access.
- **Bug fixed (owner reported "final price stays 0 after pricing a cake")**: Order Detail's Final Price input was seeded once from `order.final_price` at mount and never re-synced — pricing a line item and saving updated the real items subtotal but not the Final Price *field*, since that's independent client state. Fixed by adjusting `finalPrice` during render (React's documented "sync state to a changed value" pattern, same convention as the Cakes admin search fix) whenever `order.final_price` is still `null` (no explicit override saved yet) and the live items subtotal has changed — so it now tracks the subtotal live until the admin actually types their own override.
- **Owner request: admin can edit an order's contact/fulfillment details without touching the customer's saved account.** New "Customer & Fulfillment" section in the Admin Actions panel: Contact Name, Phone + Contact Method, Address, Fulfillment Method (Pickup/Delivery), Delivery Area (selecting one auto-fills the Delivery Fee field from that area's price, still overridable), Fulfillment Date, Order Notes — all saved by the same "Save Changes" button as status/pricing. Contact Name reuses `orders.guest_name` as a per-order display override even for account orders (that column was otherwise unused once an order has a `customer_id`) rather than adding a new column — deliberate reuse, not a new schema decision. `orders.delivery_area_id` and a fetched `delivery_areas` list are now passed down to `OrderDetailContent` for this.
- **Owner request: customer self-cancel from Profile > Order History.** New `cancel_own_order(p_order_id)` RPC (`20260817110000_cancel_own_order_rpc.sql`, `security definer`, self-checks `customer_id = auth.uid()` and `status = 'pending'`) — same narrow-RPC-instead-of-RLS-policy pattern as `create_order`. `OrderHistoryList` shows a real "Cancel Order" button (confirm-gated) for pending orders; any other status shows a "contact us to cancel" prompt with WhatsApp (prefilled with the order number, reusing `buildOrderWhatsAppUrl`) and Instagram links instead.
- **New shared `ReferenceImageViewer`** (`src/components/ui/reference-image-viewer.tsx`): a clickable thumbnail that opens a fullscreen `Dialog` view with a "Download to device" button (fetches the Cloudinary URL as a blob client-side rather than a plain `<a download>`, since that attribute is ignored cross-origin) and, when an `onRemove` callback is passed, a remove button gated behind the shared `ConfirmDialog`. Replaces three separate inline `next/image` blocks that had no fullscreen/download/confirm affordance: `CakeItemFields`' `ReferenceImageField` (admin edit form), `OrderLineItem`'s read-only view (accountant), and the storefront `OrderDetailModal` (customer's own Profile → Order History → View Details).

### New Order / manual entry (`/admin/orders/new`) — done

`src/app/[locale]/(admin)/admin/orders/new/page.tsx` (server, admin-only: fetches every cake — active **and** inactive, per the spec's searchable-select requirement — plus a `catalogByCategoryId` map built for **every** category up front, not just categories already referenced by an order like Order Detail does, since the admin can pick any cake from any category interactively) → `NewOrderContent` (Source/Customer Details/Order Items/Order Notes + Fulfillment/Order Summary sidebar) → `NewOrderItemRow` per repeatable item row → `CakeSelect` (new searchable combobox) and, when "+ Add customization details" is expanded, the same `CakeItemFields` from Order Detail.

- **`CakeSelect`** (`src/components/admin/orders/CakeSelect.tsx`): a plain filtered-list-behind-a-text-input component, not a new combobox dependency — the admin catalog is dozens of cakes, not thousands. Lists a "Custom / Other (describe in notes)" option first (`cakeId: null`), then every cake (active and inactive, inactive ones visually dimmed with a label) filtered by EN/AR substring match.
- **"Custom / Other" has no backing `cakes` row**, so `order_items.cake_id` had to become nullable (`20260816210000_manual_order_entry.sql`) — its description goes straight into that line item's own `notes` field (`NewOrderItemRow` swaps in a plain textarea instead of "+ Add customization details" when `cakeId` is null), per the Figma option label itself ("describe in notes"), not a new dedicated column.
- **`order_items_fake_cake_fields` relaxed**: the original constraint (Phase 2) mandated `size_id`/`shape_id` on every non-fake item, which was always true through the storefront (`CakeCustomizer` blocks add-to-cart until they're set) but isn't true here — New Order's customization panel is optional per the Figma annotation, so a bare cake + qty + price row must be insertable. The constraint now only enforces the real-cake-vs-Fake-Cake mutual exclusivity (a non-fake item can't carry fake fields, a fake item must carry its own required ones), not that every real-cake field is filled in.
- **`orders.email`** (new column): Customer Details' optional Email field had nowhere to go — manual orders are always guest-shaped (`customer_id` null; there's no concept of linking a phone/DM order to an existing account in this flow), so there's no `profiles.email` to fall back on either.
- **`create_manual_order` RPC** (admin-only, `security definer`, same self-check pattern as `create_order`/`update_order_item_customization`): rejects any `p_source` other than `phone`/`instagram`/`in_person` (no `website` from this path) and requires `guest_name`/`contact_phone`. `normalize_order_on_insert` still fires on its `orders` insert same as any other order (forcing `status='pending'`, `final_price=null`, `source='website'`) — rather than special-casing that trigger for this one caller, the function does a plain insert and then immediately `UPDATE`s the same row's `source` and `final_price` (the trigger only fires `before insert`, not `before update`). `final_price` is set directly to the computed total at creation, not left as an estimate — these prices were already agreed with the customer over the call/DM, unlike a website order awaiting admin confirmation.
- **Fulfillment/Delivery Date reuse**: `FilterChip` (Link/URL-driven, built for category-browse pages) doesn't work for in-form toggle state — this screen uses `SelectChip` throughout (Source, Pickup/Delivery, Delivery Area), the same real-button component `CakeCustomizer` uses, per the existing `ToggleChip` precedent in ARCHITECTURE.md's Phase 4 notes ("`FilterChip` is `Link`/URL-driven... isn't usable inside a form"). Delivery Date reuses the storefront's `DatePicker` as-is (tomorrow-minimum, `delivery_calendar_blocks`-aware) behind a small popover — no admin-specific relaxation of the minimum date, since nothing in the spec asked for same-day/backdated entry.
- **Open product decision, not resolved this pass**: manual orders default to `status = 'pending'` like every other order (admin can flip to Confirmed immediately via Order Detail after creating) rather than auto-confirming, since the New Order screen has no status field of its own and defaulting to the same lifecycle as every order avoided guessing at a product decision the spec didn't make. Worth revisiting with the owner — arguably a manually-entered phone/DM order is already confirmed by definition.
- **Not built this pass, matches the "Open / to be decided" note already in this doc**: promo codes aren't offered on New Order (the source screens don't show one, and none of the manual-entry Figma frames or task text mention it).
- **Post-launch fix**: the Remove button on an Order Item row was plain text with no button affordance and silently no-op'd when it was the last remaining item (owner feedback). `NewOrderItemRow` now renders it as a real bordered/hover-styled button (matching the destructive-icon convention elsewhere in admin, e.g. `RowActions`) and shows an inline "an order needs at least 1 item" message instead of doing nothing when removal is blocked.
- **Post-launch bug fix (2026-08-18): `create_manual_order` failed on every call.** `RETURNS TABLE(id uuid, order_number text)` implicitly declares `id` as a variable in the function's own plpgsql namespace; the function's post-insert `UPDATE public.orders ... WHERE id = v_order_id` referenced that bare `id`, which Postgres couldn't disambiguate from `orders.id` ("column reference \"id\" is ambiguous"). Every attempt to create a manual order threw this before ever reaching the front end's error message. Fixed by qualifying it as `public.orders.id`, matching every other `id` reference already in that function (`20260818090000_fix_create_manual_order_ambiguous_id.sql`).
- **Post-launch bug fix (2026-08-18): Fake Cake was unreachable from New Order.** `CakeItemFields` (shared by New Order's "+ Add customization details" panel and Order Detail's line-item edit) never rendered a Cake Type toggle at all — `isFake` could only ever be `false` on a newly created item, regardless of category. Added an opt-in `allowFakeCake` prop to `CakeItemFields` that renders the same Normal/Fake Cake `SelectChip` pair `CakeCustomizer` uses (and resets the type-specific fields on switch, matching `CakeCustomizer`'s `switchCakeType`); the prop is omitted on Order Detail's edit on purpose (an existing item's type still stays fixed once placed — see the note above), and New Order computes it the same way the storefront does: `cake.allow_fake && cake's top-level category isn't Bento/Candy Corner` (`NO_FAKE_CAKE_SLUGS`, duplicated locally in `admin/orders/new/page.tsx` since New Order builds its own catalog context server-side rather than reusing the storefront's cake detail page).
- **Post-launch feature (2026-08-18): second phone number, Instagram username, and fully-optional contact fields on New Order.** Owner request: an admin taking a manual order doesn't always have every contact detail on hand. `NewOrderContent`'s Customer Details gained a second Phone Number field (same `ToggleChip` call/WhatsApp/both pattern as Phone Number 1) and, shown only when Order Source is Instagram, an Instagram Username field — both optional, alongside the primary phone which is now optional too (previously required). `orders` gained `contact_phone_2`, `contact_phone_2_method`, `instagram_username` (all nullable; `20260818100000_manual_order_phone2_instagram.sql`), and the `orders_check` constraint (`customer_id is not null or (guest_name is not null and contact_phone is not null)`) was relaxed to drop the `contact_phone` requirement — a guest-shaped order now only needs `guest_name`. This is a schema-level relaxation only: the storefront's own guest checkout still requires `phone1` client-side (`CheckoutPageContent`'s `fieldErrors`), unchanged. `create_manual_order`'s signature grew three params accordingly (`p_contact_phone_2`, `p_contact_phone_2_method`, `p_instagram_username`) — since `CREATE OR REPLACE FUNCTION` can't change a parameter list, the migration drops the old 12-param overload before creating the new 15-param one. Order Detail's read-only Contact & Address panel shows the two new fields when present (no edit UI for them yet — out of scope for this pass, matches the existing pattern where `OrderDetailContent`'s "Customer & Fulfillment" edit block covers what it already covered).
- **Post-launch fix (2026-08-18): delivery address is now conditionally required, not always required.** Owner request: a pickup order shouldn't force an address. This was already true for New Order (`NewOrderContent`'s validation only checked `address.trim()` inside the `fulfillmentMethod === "delivery"` branch); the storefront Checkout's `fieldErrors.address` was unconditional and got the same `fulfillmentMethod === "delivery" &&` guard, with a helper hint under the field when Pickup is selected. `deliveryAddress` is passed as `null` (not an empty string) when left blank; `CreateOrderInput.deliveryAddress` widened to `string | null` to match (`orders.delivery_address` was already nullable — no schema change needed). Order Confirmation's Address row is now conditionally rendered (hidden when there's no address, e.g. a pickup order that didn't supply one) instead of always showing, which would otherwise render as a blank line.

### Delivery Areas (`/admin/delivery-areas`) — done

Same CRUD/temporary-disable table pattern as Colors/Sizes/Flavors/Toppers (Phase 5) — `DeliveryAreasPageContent` + `DeliveryAreaFormDialog` mirror `ColorsPageContent`/`ColorFormDialog` almost exactly (name EN/AR, one extra numeric field instead of a hex picker, `Switch` for Active, `RowActions` for edit/delete). No new schema or RPC — `delivery_areas` already had `active`/`sort_order` from Phase 2, and admin already has full RLS access via the existing `admin_all` policy.

### Delivery Calendar (`/admin/delivery-calendar`) — done

`DeliveryCalendarPageContent`: a new month-grid calendar distinct from both the storefront `DatePicker` (single-select, blocks past dates + already-blocked dates, used by a customer) and the admin `DateRangeFilterButton` (range-select, no past-date restriction, used for filtering) — this one single-click **toggles** a date blocked/unblocked directly against `delivery_calendar_blocks` (insert to block, delete to unblock), with past dates always disabled and today marked with a border ring. The Blocked Dates side list mirrors the same data with a Remove action. Parses `blocked_date` (`YYYY-MM-DD`) via explicit local-component construction rather than `new Date(iso)`, which parses as UTC midnight and can display as the previous day in timezones behind UTC — Cairo is ahead of UTC so this specific bug wouldn't have shown up in testing, but the safer parsing (already used by `DateRangeFilterButton`) was applied here too rather than relying on that coincidence. No new schema — `delivery_calendar_blocks` and the storefront's own blocked-date checks (Cart, Checkout, New Order) were all already wired to this table.

### Promo Codes (`/admin/promo-codes`) — done

`PromoCodesPageContent` + `PromoCodeFormDialog`: same table/dialog CRUD pattern, with two admin-specific pieces — a Discount Type toggle (`SelectChip` Percentage/Fixed Amount, not a dropdown, matching the Filter-Chip-style toggles used throughout Phase 6) and a Redemptions column (`{used} / {cap}`, `∞` when `redemption_cap` is null) computed by fetching all `promo_code_redemptions` rows once server-side and counting per `promo_code_id` in JS — no new aggregate query or RPC, matching this project's existing "fetch and reduce client-side" convention for small admin datasets. Redemption cap **enforcement** itself was already built in Phase 4 (`validate_promo_code` RPC, checked at Checkout) — this screen is visibility/management only, no new enforcement logic. **Phase 7's Promo Codes analytics tab reads the same `promo_code_redemptions` table for its ROI reporting — no schema change needed there either.**

## Phase 7 — Admin: money

Figma design confirmed for all of the below — same file (`UR2u2vVxduNHFheGewn9CH`). Built across one extended session: Dashboard-Home first, then Expenses, then Analytics (5 tabs on one page + 3 drill-down pages). See the TASKS.md Phase 7 checklist for the page-by-page build order and the implementation prompt handed to Claude Code for this phase.

**New Figma pages this phase** (all under the same file):

- `Admin - Dashboard` (dashboard-home, replaces `/admin`'s plain welcome screen)
- `Admin - Expenses`
- `Admin - Analytics` — **one Figma page containing five separate root frames side by side**, one per tab (Revenue & Profit, Orders & Fulfillment, Catalog Performance, Customers, Promo Codes) — each frame is a full standalone mockup of that tab's state (correct tab highlighted, correct Period chip selected), not a toggle/variant on a shared frame. Read whichever frame matches the tab you're implementing; the sidebar/topBar/tab-bar/period-row markup is identical across all five and was built by literal duplication in Figma, so implement it once as a shared layout component in code rather than five copies.
- `Admin - Analytics - Revenue Detail` (drill-down, linked from the Revenue & Profit tab)
- `Admin - Analytics - Catalog Detail` (drill-down, linked from the Catalog Performance tab)
- `Admin - Analytics - Customers List` (drill-down, linked from the Customers tab)

**Sidebar**: MONEY section's `Expenses` and `Analytics` nav items were switched from `State=Disabled` to `State=Default` in both the Expanded and Collapsed sidebar masters — same mechanism used to light up ORDERS at the start of Phase 6. No `/admin` top-level "Dashboard" nav item was added; the existing header wordmark/home-icon in `AdminSidebarNav` is the way back to `/admin`, same as before.

**Every screen this phase has one or more yellow annotation-note frames** (same `#fff5c7`/`#f0bf26`/`#805900` convention as Phase 6) spelling out exact data sources, routing, and edge cases — read every note on every frame before writing the query behind it, they're not decorative.

### Dashboard-Home (`/admin`, replaces the Phase 5 plain welcome screen) — designed

Server component, `requireStaff` (admin or accountant both land here, matching the `/admin` layout guard already established in Phase 6).

- **Greeting**: "Welcome back, {name}!" (reuses the Phase 5 welcome-screen name lookup) + a static subline.
- **Three stat cards**, each with its own action button(s) wired to a specific destination — **do not build these as static display-only cards**, the buttons are required scope:
  - **Today's Orders** — count + "orders placed today" subtext. Buttons: **View All Orders** → `/admin/orders`, no filter. **Filter: Today** → `/admin/orders` with the Order Date range preset to today only.
  - **Pending Orders** — count + "need your attention" (warning-orange subtext, matching the existing `--status-pending` token). Buttons: **View All Orders** → `/admin/orders`, no filter. **Filter: Pending** → `/admin/orders` with the Status chip preset to Pending.
  - **Revenue This Month** — amount + a trend line ("+18% vs last month", success-green). Button: **View Revenue (This Month)** → `/admin/analytics`, Revenue & Profit tab, Period preset to the current calendar month.
- **Upcoming Deliveries** panel: next-7-days list (date, order #, customer/guest name, fulfillment method + area, a per-row "View" linking to that order's Order Detail page) + a "View all orders →" link at the top of the panel.
- All three stat-card buttons and the deep-links above **reuse the existing `/admin/orders` list and its filters (Phase 6) and the new `/admin/analytics` page (this phase) — no new routes or pages for the Dashboard itself**, it's a client of both.

### Expenses (`/admin/expenses`) — designed

Admin-only (`requireAdmin`) — Accountant has read access to _analytics_ on expenses (see Roles/Phase 7's Analytics section) but no entry/edit rights, matching the existing catalog-write pattern (Accountant reads, never writes).

- Same CRUD table pattern as Colors/Delivery Areas: columns Date, Category, Amount (EGP), Notes, Actions.
- **Filters above the table**: Category filter chips (All + one chip per `expense_categories` row) and a Date Range pill button (same click-day-then-second-day range popover as `DateRangeFilterButton` from Orders) — the two combine (AND), same convention as every other multi-filter admin list in this project.
- **"+ Add Expense"** opens a modal (mirrors `ColorFormDialog`'s shape): Date (defaults today, any past date allowed — backdatable per the original task spec), Category (Select, populated from `expense_categories`), Amount in EGP (numeric), Notes (optional). Delete confirms via the shared `ConfirmDialog`.
- **"Manage Categories"** (new secondary button next to "+ Add Expense", owner-requested addition beyond the original task scope) opens a separate modal: full CRUD list of `expense_categories` — add, inline-rename, delete. **Delete must check for the Postgres FK-restrict error code (`23503`) and show a specific "can't delete — expenses still use this category" message**, reusing the exact pattern already built for `CategoriesPageContent.handleDelete` in Phase 5 rather than re-deriving it. This is now the **only** place expense categories are managed — no more direct-SQL/seed-only category management.
- **Schema check needed before building** (flagged, not resolved this session — Figma-only, no DB access this pass): confirm `expense_categories.name` is already a bilingual JSONB (`{en, ar}`) matching every other catalog-style table's convention. If it's still a plain `text` column from its original Phase 2 seed, add a migration to make it bilingual before building the Manage Categories modal, since that modal needs EN/AR inputs like every other admin category form in this app.

### Analytics (`/admin/analytics`) — designed

`requireStaff` (admin AND accountant, per the Phase 6-established pattern — analytics is explicitly listed as accountant-accessible in Roles). One page, five tabs, one shared **Period** control (Day / Week / Month / Year / Custom Range — Custom opens the same range popover as `DateRangeFilterButton`) that applies to whichever tab is active and persists across tab switches. Each tab has its own **Export to Excel** button that exports only that tab's data for the current period (not a combined workbook).

**All figures on every tab are combined across every `orders.source` by default** (website + phone + Instagram + in-person) — no source filter shown anywhere on this page, per the original "whole business, not just the website" requirement. Revenue/expense/profit figures always use `final_price` on completed orders, never the live/unconfirmed estimate.

Two recurring conventions across every tab, established once and repeated everywhere — **do not vary this per-tab**:

- **Period-scoped vs. all-time metrics**: most numbers are scoped to the selected Period, but a few are deliberately **not** — Never-Ordered Cakes (Catalog Performance) and Repeat Rate (Customers) are all-time signals that would be misleading if period-scoped (e.g. "never ordered in the last week" is a meaningless prune signal). Each such metric's annotation note in Figma says so explicitly; don't silently make everything period-scoped for consistency's own sake.
- **"View Full X" / "View All X" buttons** on every tab open one of the three drill-down pages below, or deep-link into the existing `/admin/orders` list with filters pre-set — never a new modal, never inline pagination bolted onto the summary tab itself. Keep summary tabs light; put the heavy lists on their own pages.

#### Tab 1 — Revenue & Profit

- 4 KPI cards: Total Revenue, Total Expenses, Net Profit (revenue − expenses), Average Order Value.
- Revenue chart (6-month trend) and an Expenses by Category breakdown (bars + amounts) — **both are Figma placeholders (literal rectangles), implement with `recharts`** (already in the stack, no new dependency).
- **"View Full Revenue & Expense Log"** button → `Admin - Analytics - Revenue Detail` (see below).
- **Post-launch addition (2026-08-18), owner request: a second row of 4 KPI cards**, still period-scoped and included in the same "Export to Excel" button — **Pending Orders Revenue** and **Confirmed Orders Revenue** (the sum of each status's orders in the selected period, `RevenueProfitTab.tsx`), **Expected Net Profit** (`totalRevenue + confirmedRevenue + pendingRevenue - totalExpenses` — i.e. profit if every order in-flight this period eventually completes as priced), and **Total Expected Revenue** (the same three-status sum, no expenses subtracted). The original "Total Revenue" card was renamed **Total Completed Revenue** to make explicit it's completed-orders-only now that pending/confirmed have their own cards (`totalRevenue` field name kept as-is in `RevenueProfitData` — only the translation key/label changed, to `totalCompletedRevenue`). Pending/confirmed orders frequently don't have `final_price` set yet (admin hasn't priced/confirmed over WhatsApp/phone), so their revenue uses the same `final_price ?? subtotal_estimate + delivery_price - discount_amount` estimate fallback already used for a completed order's spend on the Customers tab. "View Full Revenue & Expense Log" is unchanged — still completed-orders-only, per the request to leave it as-is.
- **Post-launch addition (2026-08-18), owner request: trend colors + vs-previous-period comparisons across all three tabs with a Period control** (Revenue & Profit, Orders & Fulfillment, Customers). General convention applied everywhere a card shows a comparison: green (`text-status-completed`) when the change is favorable, red (`text-destructive`) when unfavorable, `text-text-secondary` when there's no previous period to compare (`trendPercent` returning `null`, e.g. previous period had 0). **"All Time" hides every margin/trend line** (there's no meaningful "previous" for it) — each card falls back to its prior non-comparison subtext (e.g. Total Orders falls back to the `periodWord` text it always showed).
  - **Revenue & Profit**: Net Profit's and Expected Net Profit's margin text is now colored by the metric's own sign (red when negative/a loss, green when positive) instead of being hardcoded green. Pending Revenue, Confirmed Revenue, and Total Expected Revenue each gained a real vs-previous-period trend line (green on increase), matching Total Revenue/Total Expenses' existing convention — requires previous-period `sumOrderValueByStatus` queries for pending/confirmed (`page.tsx`).
  - **Orders & Fulfillment**: Total Orders' subtext changed from a static "this month"/"this week" label to a real vs-previous-period trend (`totalOrdersPct`, green when orders are up) — the period-word text is now only the All Time fallback. Cancelled Orders gained a second line comparing this period's cancellation *rate* against the previous period's rate as a percentage-point difference (green when the rate improved/dropped, red when it worsened) — needs a lean previous-period `orders.status` query (`page.tsx`) since the existing query only ever fetched the current period. Delivery Orders and Pickup Orders are unchanged (owner explicitly said to leave the rest as they were).
  - **Customers**: "New This Period" (label renamed from the month-specific "New This Month" — the card reflects whatever Period is selected, not always a month) gained a vs-previous-period trend. **Total Customers deliberately did not** — it's a plain running total (`profiles` with `role = customer`, unbounded by Period), and the owner's call was that comparing a cumulative total against "last period's cumulative total" wasn't a meaningful trend worth showing, so it stays a plain count with its original static "with a saved account" subtext, no color, no All Time special-case.

#### Tab 2 — Orders & Fulfillment

- 4 KPI cards: Total Orders, Cancelled Orders (+ cancellation rate = cancelled ÷ total orders in period), Delivery Orders, Pickup Orders.
- Order Source breakdown and Delivery Area breakdown (counts + % of total orders in period, not revenue-weighted).
- Cancelled Orders table (a handful of example rows, order #/date/customer/total/View-link-to-Order-Detail).
- **"View All Orders (This Period)"** button (above the KPI cards) and **"View All Cancelled Orders"** button (inside the Cancelled Orders table) — **both deep-link into the existing `/admin/orders` list (Phase 6) with its Order Date range pre-set to match the Period control here**; the latter additionally pre-sets the Status chip to Cancelled. No new page for either — this tab's whole "give me the full list" need is already served by the Orders list that exists.

#### Tab 3 — Catalog Performance

- 4 KPI cards: Top Cake, Top Flavor, Top Size, Never-Ordered Cakes (count — **all-time**, see the convention note above).
- Revenue by Category breakdown (all 6 top-level categories).
- Most-Ordered Cakes table (rank/cake/category/orders/revenue) and Never-Ordered Cakes table (cake/category/added date/orders=0).
- **"View Full Catalog Performance"** button → `Admin - Analytics - Catalog Detail` (see below).

#### Tab 4 — Customers

- 4 KPI cards: Total Customers (profiles with `role = customer`, not guests), New This Month, Repeat Rate (2+ orders ÷ customers with ≥1 order — **all-time**, see convention note), Guest vs. Account order split for the period.
- Guest vs. Account Orders breakdown card.
- Top Customers table, ranked by order count within the period — **"Guest" rows are grouped by matching `guest_name` + `contact_phone` since there's no account to key on; this is an approximation, flag it as such, not exact identity matching.** Manually-entered offline orders (`source ≠ website`) are always guest-shaped per this doc's Core Entities section, so a repeat phone/DM customer will only ever show as recurring "Guest" rows here, never merge into an account.
- **A 6th Period chip, "All Time"** was originally scoped to only this tab — **owner later asked for it on every tab**, so `AnalyticsShell`'s `showAllTimeChip` prop is now `true` everywhere (including Promo Codes). Selecting it just reframes "this period" language to "all time" on the cards; the shape doesn't change.
- **"View All Customers"** button → `Admin - Analytics - Customers List` (see below).

#### Tab 5 — Promo Codes

- 4 KPI cards: Active Codes (of total), Redemptions (period), Discount Given (period), Revenue Influenced (period, + ROI%).
- Promo Code Performance table: Code, Redemptions (`used / cap`, `∞` if uncapped — same display convention as the Promo Codes admin page), Discount Given, Revenue Influenced, ROI.
  - `Revenue Influenced` = sum of `final_price` for orders with a `promo_code_redemptions` row in the period. `Discount Given` = sum of the _actual_ discount amount applied per order (fixed amount, or subtotal × percentage — whichever that code used), not the code's face-value rate. `ROI = (Revenue Influenced − Discount Given) ÷ Discount Given`, shown as a %. This is directional, not causal — it doesn't claim the order wouldn't have happened without the code.
  - **Per-row "View Redemptions"** expands an inline mini-table (order #, customer/guest name, date, discount applied on that order) sourced directly from `promo_code_redemptions` joined to `orders` — no new table. Suggest only one code's redemptions expanded at a time; not a hard requirement.
- Redemption cap **enforcement** already exists (`validate_promo_code` RPC, Phase 4) — this tab is read-only reporting only, same relationship the Promo Codes admin page already has to that RPC.

### Drill-down page: Revenue Detail (`Admin - Analytics - Revenue Detail`) — designed

One merged, chronological ledger: a **Revenue** row per completed order (description = order # + customer/guest name, clickable to Order Detail, amount = `final_price`) interleaved with an **Expense** row per `expenses` entry (description = category + notes), sorted newest first, for whatever Period was active on the Revenue & Profit tab when the link was clicked (carry it via query params, don't require re-picking it here).

- A **Running Balance** column is a cumulative sum computed top-to-bottom over the **full, unfiltered** period result set. The "Show: All / Revenue Only / Expenses Only" filter chips are view-only — filtering must **not** recompute the balance from the filtered subset, since that would misrepresent it as a partial total.
- "Export to Excel" exports the full merged ledger regardless of the Show filter.

### Drill-down page: Catalog Detail (`Admin - Analytics - Catalog Detail`) — designed

- **All Cakes** list, server-paginated, sortable (Most Orders / Most Revenue / A–Z) and filterable by category (chips) — sort and category filter combine (AND). Ranked by performance within the Catalog Performance tab's active Period.
- Four more usage lists, each period-scoped the same way (**not** all-time, unlike Never-Ordered Cakes on the summary tab — "how popular right now" is the point of this page):
  - **Flavors Used** — counts `order_item_flavors` occurrences.
  - **Sizes Used** — counts `order_items.size_id` (real cakes) or a bucketed `fake_size_cm` label (Fake Cake items).
  - **Toppers Used** — counts topper selections. **Verify the exact column/table** for an order item's topper before writing this query (see the flag under Core Entities → `order_items` above — this doc assumed `order_items.topper_id` but it was never spelled out as its own bullet in earlier phases).
  - **Icing Colors Used** — counts `order_item_colors` rows (a 2-color cake counts once per color it used, not once per order item).
- Clicking a cake name links to its `Admin - Cake Form` edit page; clicking a flavor/size/topper/color name can link to its respective catalog admin page for context (not required to build first).
- Charts/bars on this page are Figma placeholders — use `recharts` or a plain sorted list, not literal rectangles.

### Drill-down page: Customers List (`Admin - Analytics - Customers List`) — designed

- Search bar: matches `first_name`/`last_name` or any `customer_phones.phone`, case-insensitive substring.
- Full, server-paginated list of **customers with a profile row only** (guests don't appear here — the Top Customers table on the summary tab is where the guest-matching approximation lives instead; this list is the exact, unambiguous account-holder view). Each row: name, type badge, order count, total spent (sum of `final_price` on **completed** orders only — pending/cancelled don't count), last order date.
- **Per-row "Expand"** reveals that customer's `customer_addresses`, `customer_phones`, and a paginated order history — the same underlying data the Profile page's `SavedAddressesCard`/`SavedPhonesCard`/`OrderHistoryList` already render for the customer themselves, reused here read-only for admin. Suggest only one row expanded at a time; not a hard requirement.
- **RLS decision — resolved, owner confirmed (B)**: `customer_addresses`/`customer_phones` keep their existing no-Accountant-read-policy shape (deliberate Phase 4 choice). This page itself is reachable via `requireStaff` (same as the rest of Analytics), but its address/phone Expand panel specifically gates to `requireAdmin` — the order-history part of the Expand panel stays available to Accountant, since Accountant already reads `orders` directly.

### Drill-down pages — done

All three built against their Figma frames, each reachable only via a "View Full X"/"View All X" button that carries the linking tab's active Period (`period`/`from`/`to` query params) so the drill-down opens already scoped the same way, and each links back to `/admin/analytics` with that tab+period restored via a "← Back to Analytics" button.

- **Revenue Detail** (`/admin/analytics/revenue-detail`, frame `185:2731`): merges completed orders (`final_price`) and `expenses` rows into one list sorted newest-first, with a cumulative Running Balance computed server-side top-to-bottom over that full sorted list *before* the client-side Show filter (All/Revenue/Expenses) ever touches it — filtering only hides rows, it never recomputes the balance from a subset. **Export to Excel exports whatever the Show filter currently has visible** (owner override — the Figma annotation originally called for exporting the full merged ledger regardless of the filter; changed on request).
- **Catalog Detail** (`/admin/analytics/catalog-detail`, frame `185:2877`): every cake (not just ones with period activity) sorted by Most Orders/Most Revenue/A–Z and filterable by top-level category via query params (`sort`, `category`) resolved server-side; Flavors/Sizes/Toppers/Icing Colors Used are period-scoped top-8 lists. No real pagination — the catalog is still dozens of cakes pre-launch, matching the "fetch small admin dataset, sort/filter without a library" convention used elsewhere.
- **Customers List** (`/admin/analytics/customers`, frame `185:3198`): account-holder profiles only (guests live in the summary tab's approximated Top Customers table instead), client-side search by name/phone, each row's Expand reveals period-scoped order history (both roles) and, admin-only, saved addresses/phones — the server page simply never queries `customer_addresses`/`customer_phones` when the caller is an accountant, rather than fetching and hiding client-side, so the RLS gap stays a genuine no-op for that role rather than a UI-only restriction.

### Bug fixed while building this phase (Figma-only, applies to implementation too)

A table-title clipping bug affected every list built with the "titleWrap" pattern this session (Cancelled Orders, Most-Ordered Cakes, Never-Ordered Cakes, Top Customers, Promo Code Performance) — the title text's container had a fixed height shorter than the text needed, clipping descenders. Purely a Figma auto-layout mistake (not a code bug, nothing to port), but flagging it here in case the visual "cut-off text" pattern rings a bell if a similar symptom ever shows up in the actual built UI — check for a fixed-height wrapper around a heading before assuming it's a font-loading issue.

## Phase 8 — Roles & security

**Design status: Figma design complete for the one screen built this pass.** Same file, key `UR2u2vVxduNHFheGewn9CH`, page `Admin - Admins & Roles`, root frame `191:2990`. The frame carries a single yellow annotation note (`193:3034`) that fully specifies the Add/Edit modal, the promote-by-email flow, and both guardrails — implemented as written, no deviations.

Of Phase 8's original three sub-items, only **Multi-admin account management** was built this pass. **Audit log viewer was cut from scope entirely** (owner decision, not deferred) — see the amended `audit_log` bullet under Core Entities: the table and its triggers keep auditing every admin/accountant mutation exactly as they have since Phase 2, there's just no page to browse that data and none is planned. **Admin/accountant RLS enforcement pass** remains the one open Phase 8 item, to be done separately.

### Multi-admin account management (`/admin/staff`) — done

Admin-only (`requireAdmin` — accountants have no business managing other staff accounts, matching the existing catalog-write pattern of "accountant reads, never writes" for anything outside their own domain).

- **Register-then-promote model, no invite-email flow.** A new admin/accountant doesn't get an account created for them here — they register normally via `/register` (ending up `role = 'customer'` like anyone else), and an existing admin then looks them up by email on this page and assigns a role. This replaces the manual "update `profiles.role` by hand in the Supabase SQL editor" workaround flagged back in Phase 5 with a real UI, but doesn't change the underlying model at all.
- **Table**: Name, Email, Role (badge — Admin uses the same brand-brown/15% treatment as elsewhere, Accountant reuses the `#4a6fa5` blue already established for the `confirmed` status badge), Added (date), Actions. "Added" reads `profiles.created_at` (account creation date) — there's no dedicated "promoted to staff at" timestamp, and adding one for a single column on a low-traffic internal page wasn't worth a schema change; `audit_log` technically has the real promotion timestamp buried in its `new_data` history if it's ever needed, but querying that from a page that otherwise has no audit-log integration would be scope creep the owner explicitly cut this same pass.
- **"+ Add Staff Member" / row Edit** open the same modal (`StaffFormDialog`): Email (lookup only, no name/password fields) + Role (native `<select>`, Admin/Accountant — matches the existing `SizeFormDialog` unit-picker precedent for a small fixed option set, not the dedicated shadcn `Select` reserved for pickers the Figma spec calls out explicitly). Edit pre-fills both fields from the row; submitting either Add or Edit calls the same RPC (see below) — there's no meaningful difference between "add" and "edit" once both are just "look up this email, set this role."
- **Row Delete** calls a revoke RPC (role → `customer`) via the existing `RowActions`/`ConfirmDialog` pattern. `RowActions` gained optional `deleteTitle`/`deleteMessage`/`deleteLabel` props (defaulting to its prior generic copy everywhere else) so this page's confirm dialog can say "will lose dashboard access, not delete their account" instead of the generic "will be permanently removed" wording every other admin table uses — reusing the one shared component rather than hand-rolling a second confirm-button set.
- **The current user's own row shows no Actions at all** (a plain "—", replacing `RowActions` entirely) rather than disabled buttons — since editing or deleting your own row is *always* blocked (see guardrails below) regardless of what you'd change it to, there's no valid click for that row to ever produce, so hiding the controls is more honest than disabling them.

### Guardrails — enforced at the trigger level, not just in the RPCs

The annotation explicitly asked for server-side enforcement, "not just hidden in the UI." The `admin_all` RLS policy already lets an admin `UPDATE` any row of `profiles` directly (not just through this page's RPCs) — so putting the guardrail checks only inside the new RPCs would leave a real bypass: an admin issuing a raw `supabase.from("profiles").update({ role: "customer" }).eq("id", ownId)` from the browser console would sail straight through. Instead, both guardrails were added to `prevent_self_role_change()` (`profiles.sql`, Phase 2's existing `before update` trigger, already responsible for blocking non-admins from touching `role`), replaced via `create or replace function` in the new migration:

1. **Last remaining admin can never be demoted or removed.** `old.role = 'admin' and (select count(*) from profiles where role = 'admin') <= 1` — evaluated inside the `before update` trigger, so the count still includes the row being changed at that point (its `role` hasn't actually written yet), giving an accurate "is this the only one left" read with a plain aggregate, no snapshot/locking trickery needed.
2. **A signed-in admin can never change their own row's role.** `old.id = auth.uid()` — covers both the Edit path (self-lookup by own email resolves to the same profile id) and the Delete/revoke path identically, since both ultimately reduce to "does `profiles.role` change on my own row." Stepping down requires another admin to do it, per the annotation.

Since the trigger is the actual enforcement point, the two new RPCs below are thin: they self-check `current_profile_role() = 'admin'` (same pattern as `create_order`/`update_order_item_customization`/`cancel_own_order`), do the plain lookup/update, and whatever the trigger raises propagates back through PostgREST as the RPC's error — the "No account found..." message is the one case that has to be raised in the RPC itself, before any update is attempted, since there's no row for the trigger to fire on. `messages/{en,ar}.json`'s `Admin.staff` namespace doesn't hardcode message strings for either guardrail error precisely because they surface directly from the trigger's `RAISE EXCEPTION` text — the modal renders `error.message` from the failed RPC call as-is.

### New RPCs (`20260817120000_staff_roles_rpc.sql`)

- **`admin_list_staff()`** — `security definer`, self-checks admin, returns every `admin`/`accountant` profile joined to `auth.users` for `email` (client code can never read `auth.users` directly — `profiles` has no email column of its own, see Auth section above). Ordered by `created_at` ascending.
- **`admin_set_staff_role(p_email text, p_role text)`** — `security definer`, self-checks admin, validates `p_role in ('admin','accountant')`, looks up `auth.users` by case-insensitive trimmed email; raises the exact "No account found with this email — ask them to register first, then try again" message from the annotation if nothing matches, otherwise updates that profile's `role` (subject to the trigger guardrails above).
- **`admin_revoke_staff_role(p_profile_id uuid)`** — `security definer`, self-checks admin, sets `role = 'customer'` on the given profile id (subject to the same trigger guardrails).
- All three read `auth.users`/write `profiles` as the function owner (the migration-runner role, effectively superuser on hosted Supabase) rather than the caller's own grants — the standard Supabase pattern for a `security definer` function needing `auth.users`, since `authenticated` itself has no grant on that schema.
- **No email is sent on a role change** (explicitly out of scope this pass, per the annotation) — the affected person simply sees new/removed `/admin` access the next time `requireAdmin`/`requireStaff` runs.

### Sidebar

TEAM section's "Admins & Roles" `AdminNavItem` was wired from a disabled placeholder (`href={null}`, `dimmed`) to a real link (`href="/admin/staff"`, active-state highlighting via `pathname.startsWith`) — same mechanism used to light up ORDERS (Phase 6) and MONEY (Phase 7) when those sections went live.

## Development environment

- **Hosted only, no local Docker stack** — decided 2026-08-14 after a working local setup was deliberately reverted (`supabase stop`). `.env.local` points at the hosted project (`https://yddapkhhniecjnnzrolv.supabase.co`); `npm run dev` always talks to hosted. This was tried both ways — the local stack did work at one point (Docker Desktop API-version issues fixed, all 23 migrations confirmed matching) — so if Docker workflow is ever wanted back, `supabase start` + swapping `.env.local` to `http://127.0.0.1:54321` (publishable key from `supabase status`) is the known-working path, just not the current one.
- Schema changes: write the migration file in `supabase/migrations/`, then `supabase db push` straight to hosted (no local apply step, since there's no local Postgres to apply it to). `db push` derives the hosted version number from the filename, which is what keeps them in lockstep — never hand-apply SQL to hosted outside a migration file (that's exactly how the version-number drift fixed on 2026-08-14 happened, via one-off MCP `apply_migration` calls). Because there's no local DB to sanity-check against first, prefer smaller, more reviewable migrations and check `get_advisors` after pushing.
- Known gotcha if local Docker is ever revisited: the local CLI/Docker stack did not reliably replicate the baseline `anon`/`authenticated`/`service_role` table grants that hosted Supabase projects get automatically at provisioning — a fresh local stack denied even `service_role` a plain `select`. `20260814090800_baseline_table_grants.sql` states those grants explicitly (matching hosted's actual grant set: `select/insert/update/delete`, not `all`) so this doesn't block a future local restart. If it ever recurs, it's a grants problem, not an RLS problem, when it happens to `service_role`.

## Phase 3 implementation notes

- **Cart is Zustand-only for Phase 3** (`src/store/cart.ts`, localStorage-persisted via `zustand/middleware persist`). The `cart_items` DB table (for logged-in users, per the locked Cart stack decision) is a Phase 4 concern — no account system exists yet to sync it against. `CartItem` (`src/types/cart.ts`) is shaped to map 1:1 onto `order_items`/`order_item_flavors`/`order_item_colors` so Phase 4 checkout can submit it close to as-is.
  - The store also carries **fulfillment state** — `fulfillmentMethod` (`'pickup' | 'delivery'`), `deliveryAreaId`, `fulfillmentDate` (ISO) — as a confirmed, permanent part of the cart data model (owner decision, not local/undecided): it's set on the Cart page and persists across reloads/navigation via the same `persist` middleware as `items`. Setting `fulfillmentMethod` clears `deliveryAreaId` (switching to pickup, or between delivery attempts, shouldn't carry a stale area over).
  - `CHECKOUT_ENABLED` (currently `false`) gates the "Proceed to Checkout" button independently of whether the fulfillment selection is complete — `useFulfillmentComplete()` (date always required; delivery additionally requires an area; pickup needs no area) is fully implemented and correct today even though the button stays unconditionally disabled until `CHECKOUT_ENABLED` flips to `true` once Phase 4 checkout exists to submit to.
  - The persisted store carries a `version` + `migrate` (currently: any version older than the current one resets the incompatible parts to their defaults — full reset below v1, fulfillment-fields-only reset from v1→v2). There's no real order data to preserve pre-launch, so a shape change is treated as something to reset cleanly rather than write real migration logic for — bump `version` again next time the shape changes.
- **Add-to-cart validation UX**: the button is always enabled (not disabled-until-valid) — clicking with missing required fields scrolls to the first incomplete section and shows an inline red error there, rather than silently doing nothing. Applied across `CakeCustomizer`'s `Section`/`InputField` components via `id`/`error` props.
- **Placeholder trending-cake data**: `20260814110000_phase3_placeholder_cakes.sql` seeds 4 cakes per top-level category (24 total; Candy Corner's 4 are spread one per subcategory) with `base_price = 0` and `primary_image_url` reusing that category's placeholder photo. Swap for real cakes/photos once the owner provides them — this is data, not schema, so no migration is needed to replace it, just admin CRUD once Phase 5 exists (or direct SQL/MCP in the meantime).
- **Category photos**: owner-supplied, one per top-level category, at `public/images/categories/{slug}.jpeg` (note: `.jpeg`, not `.jpg`). Referenced directly as local paths in both the DB (`cakes.primary_image_url`) and `CategoryCard`, not routed through Cloudinary — Cloudinary integration for the storefront hasn't been wired up yet (Phase 5 admin CRUD is where cake image upload actually happens). Revisit whether local `public/images` placeholders should move to Cloudinary once that's built.
- Design tokens from Figma ("YCakes — Design System" file) are now in `src/app/globals.css` as CSS variables under `@theme inline` (`--color-brand-primary`, `--color-bg-page`, etc., generating Tailwind utilities like `bg-brand-primary`) and fonts (Cairo for body/UI incl. Arabic subset, Baloo 2 for headings, Caveat for the script accent) are loaded in `src/app/[locale]/layout.tsx`. `Button` (`src/components/ui/button.tsx`) gained `brand-primary`/`brand-ghost` variants and an `xl` size matching Figma's pill-shaped CTAs — built with Base UI's `render` prop pattern (not `asChild`), e.g. `<Button render={<Link href="/shop" />}>`.
- **Figma MCP note for future sessions**: the desktop-app bridge's `get_metadata` (no nodeId) and `get_design_context` calls require an active _selection_ in the Figma desktop app at call time, not just the file/page being open — `get_metadata`/`get_screenshot` with an explicit nodeId work without a live selection, but `get_design_context` and `get_variable_defs` don't. Ask the user to click the exact frame in Figma desktop immediately before each `get_design_context` call. **Not needed for Phase 7's pages** — this session's Figma work went entirely through `use_figma` raw Plugin API JavaScript instead (see the Figma MCP workflow note carried in the Phase 7 implementation prompt), which has no dependency on desktop selection state.
- **Cart page** (`/cart`) is a server component (`page.tsx`, fetches `delivery_areas` + `delivery_calendar_blocks`) wrapping a client component (`CartPageContent`, reads the Zustand cart). The checkout button is disabled with a "coming soon" caption rather than linking anywhere, since Phase 4 doesn't exist yet. Cart item names/attribute chips are frozen as plain strings in whatever locale was active when added (not re-fetched/re-translated), so switching language after adding an item won't retranslate the cart — acceptable for a client-only cart, worth revisiting once Phase 4 persists carts server-side.
- **Pickup/Delivery + fulfillment date live on the Cart page, store-persisted** (confirmed owner decision, ahead of the rest of Phase 4 checkout — see the fulfillment-state bullet above): pickup is a single fixed location (hardcoded "New Cairo" label, not DB-driven — there's only one physical location); delivery pulls area chips from `delivery_areas` (seeded: Cairo, Giza, Helwan, all at price 0/TBD, `20260815120000_seed_delivery_areas.sql`). Date selection is a custom month-grid calendar (`DatePicker.tsx`, no calendar library dependency) disabling anything before tomorrow (no same-day reservation) and any date in `delivery_calendar_blocks`. When Phase 4 checkout is designed, it can build directly on this existing persisted state rather than needing to invent it — the open question is only whether Checkout keeps reading/writing the same cart-store fields or the picker UI itself moves to a dedicated checkout page.

## Open / to be decided in later phases

- Fake cake sizes: format is numeric cm (`fake_size_cm`, decided in the Phase 2 follow-up migration); whether any price modifier applies is still owner TBD
- Real delivery prices (owner TBD, seeds at 0) — the areas themselves are now named: Cairo, Giza, Helwan (`20260815120000_seed_delivery_areas.sql`)
- Real price modifiers for sizes/flavors/toppers/tiers/cakes (owner TBD, entered via Phase 5/7 admin UI — everything seeds at 0)
- Whether admins can edit/reassign `orders.source` after creation, once Phase 6 manual entry exists (owner TBD) — the enum values themselves (`website`/`phone`/`instagram`/`in_person`) are locked in
- Cart persistence (`cart_items` table for logged-in users, per the locked Cart stack decision) — not yet built, scheduled for Phase 3
- Forgot Password flow — Login page has the link designed but it's not wired to anything yet (owner TBD on email-reset vs. other approach)
- Real business phone number (owner TBD) — placeholder `+20 100 123 4567` in `src/lib/contact.ts`, feeds ContactModal + Footer
- Core Phase 2 schema implemented — see `docs/superpowers/specs/2026-08-13-phase2-data-model-design.md` and `supabase/migrations/`; cart persistence and saved-address storage still pending per Phase 3/4. Phase 2 follow-up migration (Dessert Cups, `fake` category removal, `order_items` fake-cake columns, `orders.source`) is done — see `20260814100000`–`20260814100400`.
- **Scheduled cleanup for orphaned/stale reference-image uploads in Cloudinary** — flagged when the customer-facing upload path was built (see "Customer-facing reference-image upload" above); needs a cron/scheduled-job mechanism this project doesn't have yet (Vercel Cron is the likely candidate), plus a decision on retention window (e.g. delete unattached uploads after N days, delete order-attached ones N days after completion or never).
- Reference-image rate limit (8 uploads/IP/10min, Postgres-based) is an unvalidated first guess — revisit with real traffic data.
- ~~Phase 7 — Accountant access to `customer_addresses`/`customer_phones` via the new Customers List page~~ — **resolved**, owner confirmed option (B): keep no Accountant RLS policy, gate the Customers List address/phone panel to `requireAdmin`.
- ~~Phase 7 — verify `expense_categories.name` is bilingual JSONB~~ — **resolved**: already `jsonb` (`20260813120900_expenses.sql`), no migration needed.
- ~~Phase 7 — verify the exact column/table for an order item's topper selection~~ — **resolved**: `order_items.topper_id` confirmed real (`20260813120700_orders.sql`), matches the assumption.
